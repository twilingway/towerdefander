## 1. Эффекты и каталог

- [x] 1.1 `packages/fx-assets/effects/shield-band.json` — луп прямой ленты энергии. Тело и ореол —
      два растянутых спрайта в противофазе (`aspect` масштабирует только X и зажат в 8), поверх
      мерцание частиц и редкие искры. Все остановки градиентов синедоминантные, а сумма аддитивных
      слоёв держится около единицы: перебор выбивает синий в циан-белый.
- [x] 1.2 `packages/fx-assets/effects/shield-impact.json` — вспышка контакта, рябь, брызги.
      Направленный: смотрит вверх, точка контакта на нижней кромке композиции.
- [x] 1.3 Категория `shield` в `FX_CATEGORIES` и `FX_CATEGORY_LABELS`, записи в `catalogue.json`.
- [x] 1.4 `shield-band` и `shield-impact` в `FX_EFFECT_IDS`; в `FX_EVENT_EFFECT_IDS` их **нет** —
      пресет их не назначает.
- [x] 1.5 Проверка: `node tools/validate.mjs` на оба, `pnpm fx:bake`,
      `node --test scripts/bake-fx-atlases.node-test.mjs`, ревью-копии в режиме `black`.

## 2. Геометрия

- [x] 2.1 `getShieldBandPoints(radius, arcHalfAngle, segments)` в `spaceshipViewModel.ts` — точки в
      системе координат слоя, центрированные на нуле.
- [x] 2.2 `getShieldBandAlpha(energy, capacity)` — заряд в плотность барьера, с ненулевым полом.
- [x] 2.3 `getShieldImpact(query)` — входное пересечение курса угрозы с окружностью щита, проверка
      попадания в сектор, внешняя нормаль.
- [x] 2.4 Тесты: точки на окружности, сектор после апгрейда, пол альфы, отказ вне сектора, отказ за
      пределами хода, отказ без курса, сектор повёрнут не на ноль.

## 3. Барьер на сцене

- [x] 3.1 `ShieldLayer` в `scene/shield.ts` вокруг существующего `drawShield`. Геометрия серпа не
      тронута, но при поднятом щите он больше не рисуется: барьер заменяет его, а не лежит поверх.
      Серп остаётся картинкой опущенного сектора и подстраховкой, пока атлас в полёте.
- [x] 3.2 Лента на `Rope`: ленивая загрузка атласа, масштаб из толщины и `frameHeight`, точки на
      `radius / scale`, альфа на вершину гасит концы сектора, `ADD`.
- [x] 3.3 Пересчёт точек только при смене формы; поворот каждый кадр.
- [x] 3.4 `pose()` — слой отвечает за то, что нарисовал.
- [x] 3.5 Проводка в `SpaceshipScene.ts` не длиннее прежней (файл 498 из 500).

## 4. Попадание

- [x] 4.1 `scene/shieldImpact.ts`: `resolveShieldImpact` с гейтами на поднятый сектор, наличие позы
      и наличие курса; окно приёма — ход за два патча.
- [x] 4.2 Слот `blockEffect` в `CombatVisual`, заполняется при создании только для враждебных
      снарядов; ветка удаления играет вспышку.
- [x] 4.3 `shieldPose` в `ReconcileRequest`, заполняется из слоя щита.
- [x] 4.4 Категория `shield` в `SPAN` и своя глубина (16, над барьером); пол размера, как у дульной
      вспышки.
- [x] 4.5 Тесты: точка на барьере, отказ при опущенном секторе, отказ без позы, отказ без курса,
      сектор после апгрейда, id эффекта совпадает с запечённым.

## 5. Радар

- [x] 5.1 `REDRAW_INTERVAL_MS` = `PATCH_INTERVAL_MS` вместо 50 мс, с причиной в комментарии.
- [x] 5.2 `node scripts/bench-panels.mjs` до и после: радар стал перерисовываться на 50 % чаще, и
      цена этого должна быть измерена, а не оценена.

## 6. Приёмка

- [x] 6.1 `pnpm check` с явным перехватом кода возврата и `pnpm spec:validate`.
- [x] 6.2 `shieldGlow.spec.ts` внутри гейта — арт-гейт: упавший счёт синих пикселей значит, что
      барьер перестал читаться как щит, и правится арт, а не порог.
- [x] 6.3 `node scripts/profile-display.mjs --cpu=4 --wave=8` до и после.
- [ ] 6.4 Приёмка глазами: барьер на поднятом щите, вспышки от попаданий врагов в сектор, барьер
      слабеет к концу заряда, опущенный щит без барьера.
