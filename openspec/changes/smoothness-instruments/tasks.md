# Tasks

## 1. Стоимость шага на проводе

- [x] 1.1 Поле стоимости шага в display-ветке состояния и в display-проекции; поднять
      `PROTOCOL_VERSION`. Проверка: контроллерная проекция не изменилась.
      `pnpm --filter @spaceship-defender/protocol exec vitest run src/index.test.ts`
- [x] 1.2 Измерение вокруг самого шага в цикле комнаты, публикация одного шага при нескольких за
      срабатывание, ноль при остановленной симуляции. Проверка:
      `pnpm --filter @spaceship-defender/server exec vitest run src/rooms/SpaceshipDefenderRoom.test.ts`
- [x] 1.3 Адаптер дисплея читает новое поле. Проверка:
      `pnpm --filter @spaceship-defender/display exec vitest run src/roomView.test.ts`

## 2. Счётчик трафика

- [x] 2.1 Чистая часть счётчика: накопление байт, скорость за окно в секунду, суммы за сеанс.
      Проверка: юнит-тест на последовательность отсчётов.
      `pnpm --filter @spaceship-defender/display exec vitest run src/model/trafficMeter.test.ts`
- [x] 2.2 Привязка к сокету живой комнаты слушателем на приём и подменой инстансного `send` на
      отправку, с признаком неудачи вместо молчаливого нуля и перепривязкой после reconnect.
      Проверка: подставной сокет в тесте, и отдельный случай «сокета нет».

## 3. Панель приборов

- [x] 3.1 Компонент панели на чистых пропсах: кадр, стоимость шага, трафик, ping, число сущностей.
      Проверка разметкой через `renderToStaticMarkup`, включая строку «не зацепился».
      `pnpm --filter @spaceship-defender/display exec vitest run src/screens/SoloCockpit`
- [x] 3.2 Показ по явному запросу, скрыт по умолчанию, `pointer-events` только на кнопках. Проверка:
      касание в области панели вне кнопок доходит до стика.

## 4. Тумблер предсказания

- [x] 4.1 Подстановка предсказанных углов становится условной; переключение без переподключения и
      без изменения отправляемого ввода.
- [x] 4.2 Тест: при выключенном предсказании канвас получает авторитетные углы, при включённом —
      предсказанные, соединение и прогон в обоих случаях те же.

## 5. Замер «до»

- [ ] 5.1 Прогон 45 секунд боя с телефона через публичный адрес: худший кадр, доля рывков, стоимость
      шага, байты вверх и вниз. Записать в `docs/performance/` как базу для следующих шагов
      программы.

## 6. Проверка

- [x] 6.1 `pnpm check` с явным перехватом кода возврата и `pnpm spec:validate`.
- [ ] 6.2 Приёмка глазами: панель не мешает вести корабль, выключенное предсказание видно как
      разница на том же соединении.
