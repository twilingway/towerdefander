# Tasks

## 1. Предел

- [x] 1.1 Добавить `ROOM_MAX_CONCURRENT` в конфигурацию сервера и в `.env.example`, с проверкой
      диапазона и тестами на значение по умолчанию и на отказ невалидному.
- [x] 1.2 Считать живые комнаты процесса и отказывать в `onCreate` после предела; отклонённый запрос
      место не занимает.
- [x] 1.3 Вынести строку отказа в общую константу протокола, чтобы сервер и общий экран не
      расходились. Расширять перечисление кодов `server:error` не понадобилось: оно разбирается
      только для сообщений уже присоединившемуся клиенту, а отказ в создании летит текстом ошибки
      матчмейкинга, поэтому `PROTOCOL_VERSION` этой правкой не двигается.

## 2. Игроку

- [x] 2.1 Показать на общем экране человеческий текст вместо кода отказа.

## 3. Проверка

- [x] 3.1 Живой прогон: сервер с малым пределом, попытки создать больше комнат, освобождение места.
- [x] 3.2 `pnpm check` и `pnpm spec:validate`.
