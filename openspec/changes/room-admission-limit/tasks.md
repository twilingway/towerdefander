# Tasks

## 1. Предел

- [ ] 1.1 Добавить `ROOM_MAX_CONCURRENT` в конфигурацию сервера и в `.env.example`, с проверкой
      диапазона и тестами на значение по умолчанию и на отказ невалидному.
- [ ] 1.2 Считать живые комнаты процесса и отказывать в `onCreate` после предела; отклонённый запрос
      место не занимает.
- [ ] 1.3 Завести код ошибки `server_at_capacity` и поднять `PROTOCOL_VERSION`.

## 2. Игроку

- [ ] 2.1 Показать на общем экране человеческий текст вместо кода отказа.

## 3. Проверка

- [ ] 3.1 Живой прогон: сервер с малым пределом, попытки создать больше комнат, освобождение места.
- [ ] 3.2 `pnpm check` и `pnpm spec:validate`.
