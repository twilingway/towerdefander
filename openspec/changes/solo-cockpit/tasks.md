# Tasks

## 1. Ядро: башню несёт корпус

- [x] 1.1 Признак крепления в конфигурации симуляции, умолчание — сегодняшнее поведение, проверка в
      валидации.
- [x] 1.2 Снос угла башни и её цели на дельту курса за шаг; порядок в шаге — курс раньше башни.
- [x] 1.3 Тесты: корпус везёт башню при включённом признаке, держит мировой угол при выключенном,
      щит не закрепляется.
      `pnpm --filter @spaceship-defender/game-core exec vitest run src/spaceshipSimulation.test.ts`

## 2. Схема и провод

- [x] 2.1 Крепление башни в блок корабля, геометрия стика в раздел руля, поднять версию файла
      баланса и дописать миграцию.
- [x] 2.2 Роль входа соло-кокпита и геометрия стика в проекции комнаты; поднять `PROTOCOL_VERSION`.
- [x] 2.3 Тесты: раздел руля не попадает в конфигурацию симуляции, крепление попадает, миграция
      старого документа не теряет таблицу волн.
      `pnpm --filter @spaceship-defender/server exec vitest run src/balance/balance.test.ts`

## 3. Комната

- [x] 3.1 Выделить вход контроллера в отдельную процедуру и собрать из неё и входа дисплея вход
      соло-кокпита; учесть третью роль соединения во всех развилках.
- [x] 3.2 Уход, возврат и истёкшее окно для соло-соединения.
- [x] 3.3 Публиковать геометрию стика на старте прогона рядом с числами руля.
- [x] 3.4 Тесты комнаты: соло получает мир и место, контроллеру в занятую комнату отказано,
      согласованный уход распускает комнату, возврат восстанавливает обе обязанности.

## 4. Общий клиентский конвейер ввода

- [x] 4.1 Перенести конвейер ввода из контроллера в `client-shared` и переключить импорты.
- [x] 4.2 Тесты контроллера зелёные без правки ожиданий.
      `pnpm --filter @spaceship-defender/controller exec vitest run`

## 5. Соло-кокпит на дисплее

- [x] 5.1 Стик с плавающим центром и мёртвой зоной долей радиуса, числа из проекции с запасными
      значениями.
- [x] 5.2 Оверлей с двумя стиками и двумя спусками поверх канваса; сетевая половина отдельным хуком.
- [x] 5.3 Выбор соло-кокпита при создании комнаты, вход с новой ролью.
- [ ] 5.4 Тесты: раздельные касания не мешают друг другу, стик не стреляет, потеря фокуса гасит оба
      потока. **Не сделано:** компонентные тесты здесь рендерятся через `renderToStaticMarkup` без
      jsdom, поэтому указатели ими не проверяются. Гарантия пока структурная — свой `PointerCycle`
      на каждый контрол — и проверяется приёмкой 8.3.

## 6. Помощь в прицеливании

- [x] 6.1 Отбор цели конусом, допуском и видимостью; подмена только точки прицеливания.
- [x] 6.2 Тесты: цель в конусе выбирается, закрытая препятствием отбрасывается, выключенная опция
      ничего не меняет.

## 7. Консоль и пресет

- [x] 7.1 Крепление башни и геометрия стика на вкладке «Управление», тест экрана.
- [x] 7.2 Пресет с величинами STEEL VOID в файле баланса.

## 8. Проверка

- [ ] 8.1 `pnpm check` с явным перехватом кода возврата и `pnpm spec:validate`.
- [ ] 8.2 Один прогон `pnpm stats:autopilot` — доказать, что бот не задет.
- [ ] 8.3 Приёмка глазами с телефона: проехать, отстреляться из обоих стволов, убедиться, что при
      развороте корпуса прицел едет с ним и оба стика работают одновременно.
