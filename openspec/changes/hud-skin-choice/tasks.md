## 1. Рамки

- [x] 1.1 Семь рамок примера в `packages/sprite-assets/sources/hud/`
- [x] 1.2 `scripts/build-sprite-assets.mjs`: род `hud` — уменьшение до двойного экранного, пауза в
      `ui-status` вырезана обнулением альфы, WebP q85; записи манифеста
- [x] 1.3 Node-тест: байты совпадают с манифестом, файл не больше 160 КиБ

Проверка: `pnpm sprites:build` дважды подряд даёт пустой `git diff`;
`node --test scripts/build-sprite-assets.node-test.mjs`.

## 2. Прототип и замер

- [x] 2.1 Рамка состояния в DOM за `?hudspike=dom`: картинка рамки, 41 сегмент (11 у пушки, по 10 у
      остальных), сравнение по числу горящих
- [x] 2.2 Рамка состояния в Phaser за `?hudspike=phaser`: `Image` рамки, 41 `Image` сегмента, `Text`
      только при смене строки
- [x] 2.3 Флаг варианта в `scripts/bench-panels.mjs`; по три прогона классики, DOM и Phaser с
      `--cpu=4 --plain --wave=5` на одном стенде
- [x] 2.4 Таблица чисел и выбор в design.md; проигравший вариант и флаг удаляются. Если выбран
      Phaser — остановиться и получить явное согласие на правку границы в `AGENTS.md`

Проверка: таблица в design.md, в каждой строке — медиана fps и доля рваных кадров.

## 3. Пресет, ядро, сервер, провод

- [x] 3.1 `HUD_SKINS` и `hudSkin` в `balanceTuningSchema` (и настройках матча, если матч в объёме);
      поле конфига в game-core
- [x] 3.2 Миграция `BALANCE_FILE_VERSION` +1 со значением `classic`; тест; seed
- [x] 3.3 Поле `hudSkin` в display-only состоянии, проекция в обеих комнатах; `PROTOCOL_VERSION` +1
- [x] 3.4 Разбор в `wire.ts` → `roomView.ts` → `parts.ts` с запасным `classic`; фикстуры

Проверка: `pnpm --filter @spaceship-defender/protocol exec vitest run`,
`pnpm --filter @spaceship-defender/server exec vitest run src/balance`, `pnpm typecheck`.

## 4. Дисплей

- [x] 4.1 `data-hud-skin` на оболочке; `HUD_SKIN_PARTS` — таблица частей; `BattleStage` берёт из неё
- [x] 4.2 Рамка сведений кампании (волна с фазой, счёт с числами поля, кредиты со строкой улучшения)
      и матча (живы / сбито / место); рамка таймера (срок волны, окно добычи) и полоса босса под ней
- [x] 4.3 Рамка состояния на четыре полосы (нагрев — накопленный) с тем же `data-testid`-контрактом;
      кнопка скана матча на месте паузы, не меньше 48×48
- [ ] 4.4 Три режима раскладки — общий экран, десктоп, короткий ландшафт (высота до 520px) — с
      долями высоты из design; safe area телевизора
- [x] 4.5 Радар в рамке: справа снизу над правой колонкой управления с кокпитом, в углу без него;
      канвас задаётся шириной, а не `transform`; коробка элемента вмещает крылья рамки (из-за
      `contain: paint`); классика не меняется
- [x] 4.6 Стики: кольцо и ручки примера под `[data-hud-skin="frame"]`
- [x] 4.7 Состояния: перегрев, опущенный щит, низкий корпус, окно добычи, босс — с текстовой меткой,
      а не только цветом; пульсация выключается при `prefers-reduced-motion`
- [x] 4.8 Тесты разметкой: сегменты по долям (0, половина, ёмкость), нет паузы, те же `data-testid`,
      метки состояний

Проверка: `pnpm --filter @spaceship-defender/display exec vitest run src/screens/RoomScreen`.

## 5. Консоль

- [x] 5.1 «Оформление HUD» на «Директоре» с подсказкой «со следующего боя»
- [x] 5.2 Тест разметкой

Проверка: `pnpm --filter @spaceship-defender/admin exec vitest run`.

## 6. Приёмка

- [ ] 6.1 `pnpm check` с явным перехватом кода возврата; `pnpm spec:validate`
- [ ] 6.2 `bench-panels.mjs --cpu=4 --plain --wave=5`: три прогона классики и три рамок, числа в
      design.md
- [ ] 6.3 Ручная приёмка: радар над правым стиком на телефоне в ландшафте, в углу на общем экране;
      полосы в порядке «пушка, корпус, щит, пулемёт»; нет паузы; таймер в рамке; ручки голубая и
      оранжевая
