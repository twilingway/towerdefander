# Tasks

## 1. Схема и миграция

- [x] 1.1 Добавить радиус арены в схему баланса, поднять `BALANCE_FILE_VERSION` и записать прежнюю
      версию в список legacy.
- [x] 1.2 Заполнять радиус из дефолтов в миграции, не перенося документ целиком.
- [x] 1.3 Тест хранилища: пресет предыдущей версии без радиуса грузится с сохранившейся таблицей
      волн. `pnpm --filter @spaceship-defender/server exec vitest run src/balance/balance.test.ts`

## 2. Конфигурация симуляции

- [x] 2.1 Выводить `worldWidth` и `worldHeight` из радиуса при сборке конфигурации.
- [x] 2.2 Задать декорации долями мира, чтобы раскладка следовала за радиусом.
- [x] 2.3 Тесты ядра и комнаты на согласованность геометрии.

## 3. Консоль

- [x] 3.1 Поле радиуса арены с подсказкой, что площадка меняется со следующего прогона.
- [ ] 3.2 Тест экрана консоли.

## 4. Проверка

- [x] 4.1 `pnpm check > log 2>&1; echo "EXIT:$?"` и `pnpm spec:validate`.
- [ ] 4.2 Приёмка глазами: арена ощутимо больше, корабль и враги ведут себя как прежде.
