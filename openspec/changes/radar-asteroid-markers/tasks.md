## 1. Протокол

- [x] 1.1 Добавить `asteroidOriginSchema` и поле `origin` в `publicAsteroidViewSchema`, поднять
      `PROTOCOL_VERSION` до 20 и обновить тесты рукопожатия.

## 2. Сервер

- [x] 2.1 Добавить `origin` в `AsteroidState` и заполнять его в `syncAsteroid`.

## 3. Дисплей

- [x] 3.1 Провести `origin` через сетевой тип астероида в `roomView`.
- [x] 3.2 Рисовать астероиды точками под маркерами кораблей, волновые заметнее фоновых.
- [x] 3.3 Обновить тест радара: он утверждал обратное — что астероидов на мини-карте нет; сначала
      убедиться, что новый тест краснеет без маркеров. Проверить
      `pnpm --filter @spaceship-defender/display exec vitest run src/CombatRadar.test.tsx`.

## 4. Проверка

- [x] 4.1 `pnpm check` и `pnpm spec:validate`.
- [ ] 4.2 Приёмка глазами: волновые камни на радаре отличимы от фоновых на реальном прогоне.
- [x] 4.3 Показать волновые камни счётчиком рядом со счётом в HUD дисплея и добавить в
      preview-фикстуру камень каждого происхождения, чтобы обе разновидности были видны без боя.
