# Tasks

- [x] 1.1 Извлечь шесть PNG из демо в `apps/display/public/textures/` (stars, dust, четыре
      туманности). Проверка: файлы на месте, размеры ~29 КБ / 4 КБ / 374–510 КБ.
- [x] 1.2 Добавить `backgroundTuningSchema` в схему баланса и поднять версию файла 15 → 16 с
      миграцией (полевой merge из дефолтов). Проверка:
      `pnpm --filter @spaceship-defender/protocol test`.
- [x] 1.3 Провести обязательное поле `background` через `displayGameSnapshotSchema`, поднять
      `PROTOCOL_VERSION` 24 → 25, обновить фикстуры протокола. Проверка: тот же тест + typecheck.
- [x] 1.4 Добавить presentation-only `background` в конфигурацию симуляции game-core с дефолтом.
      Проверка: `pnpm --filter @spaceship-defender/game-core test`.
- [x] 2.1 Мигрировать раздел фона в `store.ts` и нести его через `toSimulationConfig`. Проверка:
      тесты баланса сервера — пресет v15 получает дефолты, новый файл сохраняет значения.
- [x] 2.2 Добавить четыре поля в `SpaceshipDisplayState` и публиковать их из активного пресета на
      старте забега. Проверка: тест комнаты — снапшот несёт фон пресета; смена пресета не трогает
      идущий забег.
- [x] 3.1 Вынести математику смещения слоёв в чистую функцию `spaceshipViewModel.ts` (факторы,
      дрейф, zoom) и покрыть тестами без Phaser: параллакс 0 глушит только камеру, дрейф живёт;
      туман B едет против камеры по X. Каждый обязан краснеть до правки.
- [x] 3.2 Нарисовать четыре TileSprite под ареной (глубины −4…−1), грузить текстуры в preload,
      обновлять `tilePosition` каждый кадр и альфы из снапшота; смена пресета — `setTexture`.
- [x] 3.3 Сделать заливку круга арены полупрозрачной (альфа 0.5).
- [x] 3.4 Провести фон через `roomView.ts`, превью и фикстуры дисплея; слои пересчитывают размер при
      ресайзе. Проверка: `pnpm --filter @spaceship-defender/display test`.
- [x] 3.5 Размещать слои по прямоугольнику покрытия кадра (позиция + размер), а не по мировому нулю:
      при zoom не равном 1 экранный ноль не совпадает с мировым, и у края кадра остаётся полоса
      пустого космоса. Проверка:
      `pnpm --filter @spaceship-defender/display exec vitest run src/game/spaceshipViewModel.test.ts`
      — тест покрытия обязан краснеть на прежнем размещении.
- [x] 4.1 Карточка «Фон космоса» на вкладке Директор: сила параллакса, автодрейф, непрозрачность
      туманности, выбор пресета из четырёх. Проверка: `pnpm --filter @spaceship-defender/admin`
      typecheck + сохранение значения в файл пресетов.
- [x] 5.1 `pnpm check` с явным перехватом кода возврата и `pnpm spec:validate`.
- [ ] 5.2 Приёмка глазами: фон виден по всему экрану, слои едут с разной скоростью, в простое —
      дрейф; на Android TV без просадок FPS.
