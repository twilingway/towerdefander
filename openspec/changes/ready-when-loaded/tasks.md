## 1. Протокол

- [x] 1.1 `loadsAssets: z.literal(true).optional()` в опциях входа и создания дисплея и соло;
      `clientMessage.assetsReady` и его strict-схема `{ protocolVersion, roomId }`; `assetsPending`
      и `assetsWaitSecondsRemaining` в виде комнаты дисплея; `awaitingAssets` в `arenaLobbySchema`;
      `PROTOCOL_VERSION` 70. Проверка: `pnpm --filter @spaceship-defender/protocol test`.

## 2. Сервер: кампания

- [x] 2.1 Готовность ресурсов по сессии: объявление при входе дисплея и соло, приём
      `client:assets-ready` с проверками, забывание при окончательном уходе. Проверка: тесты
      комнаты.
- [x] 2.2 `tryStartRun`: ожидание загрузки, таймер 20 с и его снятие, rematch без повторного
      ожидания, поля состояния. Проверка: тесты комнаты — экипаж готов и экран грузится, экран
      догрузился, прошло 20 с, SDK-дисплей без флага, reconnect, неверное сообщение.

## 3. Сервер: арена

- [x] 3.1 Соло с `loadsAssets` замораживает отсчёт до готовности или 20 с; `awaitingAssets` в
      `arena:lobby`; приём сообщения. Проверка: тесты комнаты арены.

## 4. Дисплей: загрузка

- [x] 4.1 `AudioBus.preload()` возвращает промис загрузки всех звуков и зовётся без жеста. Проверка:
      unit-тест на подменённых `fetch` и `AudioContext`.
- [x] 4.2 `assetWarmup.ts`: адреса из манифестов, `Image.decode()`, звуки, ошибка файла считается
      завершением; хук готовности рядом с `useRuntimePreload`. Проверка: unit-тест — готов после
      всех файлов и после ошибки одного.

## 5. Дисплей: сессия и лобби

- [x] 5.1 `useRoomSession`: `loadsAssets` при создании комнаты и матча, `client:assets-ready` после
      входа или по окончании прогрева. Проверка: `pnpm typecheck`, тест сессии, если он есть.
- [x] 5.2 Лобби: «Загружаем ресурсы… N с», кнопка соло «Загрузка ресурсов…», очередь арены.
      Проверка: тесты `LobbyLayout` и экрана арены.

## 6. Проверка

- [x] 6.1 `pnpm check` с перехватом кода возврата; `pnpm spec:validate`.
- [x] 6.2 На стенде: соло-забег — кнопка «Готов» становится доступной после загрузки, первый выстрел
      слышен. С дросселированной сетью в DevTools бой стартует не позже чем через 20 секунд после
      готовности экипажа.
