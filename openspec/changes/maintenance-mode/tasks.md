# Tasks

## 1. Контракт

- [x] 1.1 Поля признака и остатка в общем виде комнаты, константа отказа рядом с пределом комнат,
      схемы управления режимом; версия протокола сдвинута. Проверка:
      `pnpm --filter @spaceship-defender/protocol test`.

## 2. Сервер

- [x] 2.1 `DEPLOY_CONTROL_TOKEN` в конфигурации и в `.env.example`, с теми же правилами, что у
      прочих секретов. Проверка: тест конфигурации на значение по умолчанию и на превышение длины.
- [x] 2.2 Модуль режима с модульным синглтоном и маршрутом включения/выключения; без токена — только
      loopback. Проверка: тесты маршрута на отказ неавторизованному и на приём с loopback.
- [x] 2.3 Отказ в создании комнаты во время работ, там же, где стоит предел комнат. Проверка: тест
      комнаты на отказ и на то, что отказ не занимает место.
- [x] 2.4 Запрет старта и повтора в `tryStartRun`; комната, дошедшая до результата во время работ,
      закрывается с собственной причиной. Проверка: тесты на готовое лобби, на повтор и на закрытие.
- [x] 2.5 Зеркалирование признака и остатка в состояние комнаты, отсчёт раз в секунду только пока
      режим включён. Проверка: тест на убывание остатка и на его отсутствие в обычном режиме.

## 3. Игроку

- [x] 3.1 Сообщение с отсчётом на общем экране во всех фазах. Проверка: тест разметки дисплея.
- [x] 3.1a Сообщение на контроллере — на входе, в лобби и на результате, а в бою только когда
      остатка мало. Проверка: тест разметки контроллера на обе ветки.
- [x] 3.2 Человеческий текст отказа до комнаты: на экране создания и на экране входа. Проверка:
      тесты разбора причины отказа.
- [x] 3.3 Переподключение во время работ показывает актуальный остаток. Проверка: тест адаптера вида
      комнаты.

## 4. Выкатка

- [x] 4.1 Релиз включает режим до ожидания, ждёт объявленный срок, затем потолок, затем обновляется.
      Потолок и срок настраиваются. Проверка: релиз при живой комнате пишет в журнал оба срока и
      обновляется по их истечении.

## 5. Проверка

- [ ] 5.1 Живой прогон: включить режим при идущей игре, убедиться, что волна доигрывается, новая
      комната не создаётся, повтор запрещён, а сообщение видно на обоих устройствах.
- [x] 5.2 `pnpm check` и `pnpm spec:validate`.
