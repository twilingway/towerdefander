# Задачи

## 1. Хранилище мира

- [ ] 1.1 `apps/display/src/model/worldStore.ts`: изменяемый снимок, счётчик версии, набор
      слушателей, `publishWorld(view)`; тест на то, что подписчик срезa не будится чужим изменением.
- [ ] 1.2 Патч пишет в хранилище вместо `setNetworkView`.
- [ ] 1.3 `useWorldSlice(select, equals)` поверх `useSyncExternalStore`.

## 2. Корень перестаёт перерисовываться

- [ ] 2.1 `App` берёт из хранилища только структурный срез (комната, фаза, экипаж, номер прогона,
      есть ли мир) с поверхностным сравнением.
- [ ] 2.2 Боевые панели получают срезы вместо пропсов из `view`.

## 3. Разбор снимка

- [ ] 3.1 Полный `parse` в разработке, разовая проверка формы в проде.

## 4. Приёмка

- [ ] 4.1 `node scripts/bench-panels.mjs --plain --cpu=4` — рывки и худший кадр против
      `docs/performance/panels-against-the-reference-stand.md`.
- [ ] 4.2 `pnpm check`.
