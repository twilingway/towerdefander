## 1. Протокол

- [x] 1.1 `FX_LOOP_EFFECT_IDS` и `fxLoopEffectIdSchema`; `shield-impact` добавлен в событийный
      список.
- [x] 1.2 `shipEffectsSchema` и необязательный `effects` у архетипа корпуса.
- [x] 1.3 Две строки в снапшоте, `PROTOCOL_VERSION` 53 → 54, `BALANCE_FILE_VERSION` 40 → 41.

## 2. Ядро и сервер

- [x] 2.1 Два визуальных поля конфига рядом с `spaceshipVisual`, пустые по умолчанию.
- [x] 2.2 `toSimulationConfig` складывает выбор корпуса, как складывает его числа.
- [x] 2.3 Комната публикует их на display-only состоянии.

## 3. Дисплей

- [x] 3.1 Слой щита берёт барьер при создании; падение на запечённый — внутри слоя.
- [x] 3.2 Вспышка попадания берётся при создании визуала снаряда.
- [x] 3.3 Тест адаптера: обе строки доезжают до вида комнаты.

## 4. Консоль

- [x] 4.1 Общая строка слота (`components/EffectSlotRow.tsx`), второй потребитель после «Врагов».
- [x] 4.2 «Корабли»: два селекта на корпус, превью кадром, «как сейчас» сбрасывает.
- [x] 4.3 Тесты: сброс убирает блок целиком, род эффекта соблюдён, неизвестный id игнорируется.

## 5. Приёмка

- [ ] 5.1 `pnpm check` с явным перехватом кода возврата и `pnpm spec:validate`.
- [ ] 5.2 Приёмка глазами: назначить корпусу барьер и вспышку, увидеть их в бою.
