# Tasks

## 1. Пакет

- [x] 1.1 Завести `packages/game-runtime` с `package.json` и `tsconfig.json`; зависимости —
      `protocol`, `game-core`, `balance-core`.
- [x] 1.2 Перенести `crewWorld.ts`, `crewPolicy.mjs` и `crewPolicy.d.mts`, `decorations.ts`,
      `upgradeJournal.ts` без правок логики; обновить путь в `eslint.config.js`.
- [x] 1.3 Перенести `runSeed.ts`, заменив `node:crypto` на Web Crypto, который есть у обоих хостов;
      вызовы при этом не изменились.

## 2. Проекция

- [x] 2.1 Описать структурную цель проекции и фабрики элементов коллекций
      (`src/projectionTarget.ts`).
- [x] 2.2 Перенести `stateProjection.ts`, сняв зависимость от классов схемы: фабрики приходят
      параметром, сервер отдаёт свои классы из `rooms/projectionFactories.ts`.
- [x] 2.3 Заменить четвёртый параметр `projectGameState` с момента дедлайна на остаток волны в
      секундах; комната считает его от `Date.now()`.
- [x] 2.4 `SpaceshipDefenderRoom.test.ts` и `performance.test.ts` проходят **без правок** — схема
      сервера подошла под структурную цель как есть, что и проверил компилятор.

## 3. Автопилот

- [x] 3.1 Собрать `src/crewAutopilot.ts`: часы политики и список ведомых мест — явные параметры, а
      какая фаза считается рабочей, решает хост, передавая пустой список.
- [x] 3.2 Перевести комнату на общий цикл с её нынешними значениями (50 мс на тик, только бой, места
      по владельцу ввода).
- [x] 3.3 Перевести `balance-run.mjs` на тот же цикл с его нынешними значениями (тот же тик 50 мс,
      пилот и наводчик всегда, щит по своему условию, явный `nowMs`).
- [x] 3.4 Тест пакета: цикл молчит на пустом списке мест и ведёт ровно переданные.

## 4. Проверка

- [x] 4.1 `pnpm spec:validate` зелёный; `pnpm check` с явным перехватом кода возврата — всё до
      браузерного набора зелёное, в нём падает только известный флейк `solo-room.spec.ts`, который в
      одиночку проходит 3 из 3.
- [x] 4.2 A/B на одном сиде: старый цикл восстановлен во временной копии стенда и прогнан рядом с
      новым (`--seed 1 --runs 6 --level veteran`). Числа совпали полностью — wave min 4 / median 6 /
      mean 5.8 / max 7, score min 820 / median 1491 / mean 1407.8 / max 1807, length median 1726.8
      s. Опции щита на стенде теперь несут ещё и `nowMs`, но ни `planShield`, ни его помощники это
      поле не читают.
- [x] 4.3 `pnpm demo:visible` глазами: бот в браузере ведёт себя как прежде — числа и игра
      проверяются порознь.
