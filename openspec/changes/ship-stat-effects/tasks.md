# Tasks

## 1. Опорный замер до правок

- [ ] 1.1 `pnpm stats:autopilot --runs 16 --level veteran --seed 1 --crew 3 --enemy-offset 0` и
      записать сюда медиану волны, среднее и max. После change эти числа обязаны совпасть: change
      ничего не балансирует, любое расхождение — регрессия.
- [ ] 1.2 `pnpm benchmark:combat`: `pureFixedStep` p50/p95 сюда же. Пересчёт статов раз в волну не
      должен быть виден в шаге.

## 2. Тест на два конфига — пишется первым и до конца переноса красный

- [ ] 2.1 Занятой сценарий в `packages/game-core/src/shipStats.test.ts`: враги на арене, оба ствола
      стреляют до перегрева, щит поднимается и разряжается в ноль, корпус получает урон. Трасса —
      состояние после N шагов.
- [ ] 2.2 Для каждого поля `SHIP_STAT_FIELDS`: шаг с `configB`, где удвоено только это поле, при том
      же состоянии обязан дать ту же трассу (иначе поле всё ещё читается из конфига), а удвоение
      того же поля в `state.ship` обязано трассу изменить (иначе сценарий поле не задевает и первая
      половина проходит вхолостую).
- [ ] 2.3 Поля, которые сценарий физически не задевает, — явный список исключений с причиной у
      каждого прямо в тесте. Пустой список предпочтителен.

## 3. Поля, правила и форма эффекта

- [ ] 3.1 `SHIP_STAT_FIELDS` в `packages/game-core/src/shipStats.ts` — 41 числовое имя плоского
      блока корабля; `MODULE_TARGET_FIELDS` = минус `shieldRadius` и
      `headingAngularBrakingPerSecondSquared`.
- [ ] 3.2 `PLAYER_SHIP_FIELDS` в `apps/server/src/balance/migrations.ts` переводится на тот же
      список, чтобы два перечисления не разошлись. Проверка: тест, что мигратор заполняет каждое
      поле списка.
- [ ] 3.3 `ShipStatEffect` (`target`, `op`, `value`) и `SHIP_STAT_RULES` в `game-core`: минимум,
      максимум, минимальная доля от базы, округление. `fireCooldownTicks` — `ceil`, не ниже четверти
      базы и не ниже тика; `shieldArcRadians` — не больше `2π`.
- [ ] 3.4 `computeShipStats(base, effects)`: `(base + Σadd) × (1 + Σpercent) × Πmultiply`, затем
      правила. Проверка: тест на независимость от порядка и на каждое правило.

## 4. Статы в состоянии

- [ ] 4.1 `ship: ShipStats` и `purchasedUpgrades: readonly UpgradeId[]` в `CombatStateFields`, **оба
      в белом списке `pickCombatResult`**; `roleModifiers` и отдельный `spaceshipMaxHp` уходят.
- [ ] 4.2 Пересчёт на покупке в `applyUpgrade`: статы от базы и всего купленного, правило корпуса
      `Δ = next − prev`, переклампивание `shieldEnergy`, `cannonHeat`, `mgHeat`. Проверка: сценарии
      дельты про корпус и про упавшую ёмкость.

## 5. Перенос чтений в симуляции

- [ ] 5.1 `spaceshipSimulation.ts`: скорость, ускорение, торможение, руль, оба ствола, нагрев, щит.
- [ ] 5.2 `collisions.ts` и `simulationWeapons.ts`: дуга щита, радиусы, урон, дульный вынос.
- [ ] 5.3 `simulationCombatBridge.ts`, `loot.ts`, `combat.ts`: ёмкость щита и максимум корпуса.
- [ ] 5.4 Внутренним помощникам, которым сегодня передают только конфиг, передаются статы. Публичный
      API пакета не меняется.
- [ ] 5.5 Тест из задачи 2 зелёный, список исключений пуст или объяснён.

## 6. Каталог как данные

- [ ] 6.1 Девять апгрейдов переписываются в эффекты по таблице из design. Проверка: тест, что каждый
      даёт ровно те же числа, что и до change.
- [ ] 6.2 Подпись карты собирается из эффектов на сервере; оператор задаёт только имя. `value` на
      карте берётся из первого эффекта.

## 7. Провод и клиенты

- [ ] 7.1 `RoleModifiersState` и `publicRoleModifiersViewSchema` удаляются; `PROTOCOL_VERSION`
      растёт. Оба roomView-адаптера и оба preview-режима чистятся.
- [ ] 7.2 `purchasedUpgrades` публикуется display-only и показывается в оверлее передышки вместо
      отладочной строки модификаторов в `CrewLatency`.
- [ ] 7.3 Проекция берёт ёмкость и дугу щита, ёмкости нагрева и максимум корпуса из состояния.
- [ ] 7.4 `upgrade-votes.mjs` и `network-smoke.mjs` переводятся на статы.

## 8. Проверка

- [ ] 8.1 Повторить замер задачи 1.1 на тех же сидах. Числа обязаны совпасть; расхождение — стоп и
      разбор, а не «стало лучше».
- [ ] 8.2 `pnpm benchmark:combat` — шаг не подорожал.
- [ ] 8.3 `pnpm check` и `pnpm spec:validate` с явным перехватом кода возврата.
