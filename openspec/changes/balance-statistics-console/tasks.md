# Tasks

## 1. Счётчики в ядре

- [x] 1.1 Снять опорный замер `pnpm benchmark:combat` до правок и записать `pureFixedStep` p50/p95 в
      эту задачу — иначе сравнивать после будет не с чем. Замер до: p50 0.1343, p95 0.2686, max
      1.1057 мс.
- [x] 1.2 Завести `packages/game-core/src/runStats.ts`: тип `CombatRunStats` и чистые функции
      инкремента (попадание дружественного снаряда, попадание по кораблю, блок щита, оборот
      кредитов). Значения монотонны за ран и не сбрасываются на границе волны.
- [x] 1.3 Перенести `FriendlyWeaponSource` в `combatTypes.ts` с реэкспортом из
      `spaceshipSimulation.ts`, добавить `source` в `FriendlyProjectileLike` и `runStats` в
      `CombatStateFields`. Дополнить литералы снаряда в `circularCombat.test.ts`, `combat.test.ts` и
      `apps/server/src/benchmarks/worstCaseCombat.ts`.
- [x] 1.4 Написать **падающий первым** тест на то, что счётчик переживает шаг, и провести `runStats`
      через белый список `pickCombatResult`. Проверка: тест краснеет, если убрать строку из белого
      списка.
- [x] 1.5 Инкременты в `collisions.ts`: попадания и урон по `projectile.source`, сбитые огнём
      астероиды, заработанные кредиты; полученный урон по `candidate.kind`, блоки щита, потраченная
      на них энергия, пробой при поднятом щите. Урон обрезать по остатку здоровья цели.
- [x] 1.6 Инкременты выстрелов по стволам в `spaceshipSimulation.ts` и `creditsSpent` в
      `applyUpgrade`. Проверка: `pnpm --filter @spaceship-defender/game-core exec vitest run` —
      сумма полученного урона равна потерянному здоровью с учётом ремонта от корпусного апгрейда.
- [x] 1.7 Повторить `pnpm benchmark:combat` и сравнить с 1.1. Проверка: `pureFixedStep` p50 не
      выходит за бюджет шага. Замер после: p50 0.1398, p95 0.2792, `pureFixedStepP95AtMost2Ms`
      истинно. Дополнительно: шесть прогонов veteran на сидах 1-6 совпали с прогоном до правок
      побайтово — счётчики симуляцию не меняют.

## 2. Политика голосования

- [x] 2.1 Завести `apps/controller/scripts/upgrade-vote-policy.mjs`: свой поток
      `mulberry32(mix(seed, waveNumber))`, таблица базовых весов по `upgradeId`, `sensitivity` по
      уровню, три сигнала нужды, выбор мест по размеру экипажа и диссидент на последнем месте.
- [x] 2.2 Написать `apps/controller/scripts/upgrade-vote-policy.node-test.mjs`: бросок
      воспроизводим; диссидент — последнее место при каждом размере экипажа; карта вне оффера не
      голосуется никогда; `rookie` игнорирует состояние корабля; раненый корабль у `ace` поднимает
      долю корпусной карты на длинной серии бросков. Добавить файл в `test` контроллера. Проверка:
      `node --test apps/controller/scripts/upgrade-vote-policy.node-test.mjs`.
- [x] 2.3 Перевести `castUpgradeVotes` в `run-autopilot-stats.mjs` на политику: планировать один раз
      на оффер, пропускать роль с уже записанным голосом. Проверка: `pnpm stats:autopilot` на
      пресете сервера отрабатывает, вывод по прежнему сиду остаётся стабильным между двумя
      запусками.
- [x] 2.4 Перевести `voteAllUpgrades` в `visible-demo.mjs` на ту же политику; ожидание голоса
      сделать по-ролевым — три роли больше не шлют один `upgradeId`. Проверка: `pnpm demo:verify`,
      флаг `upgrades` истинный.

## 3. Стенд: наблюдатель, экипаж, оси, файл

- [x] 3.1 Вынести наблюдателя по-волновых величин в `apps/server/scripts/stats-observer.mjs`: дифф
      соседних состояний (убийства по архетипам и босса, прохождение и длительность волны, состав
      волны по `pendingSpawns`, потеря здоровья, фазы щита, перегревы) плюс срез `runStats` на
      границах волн. Босс-волна определяется по составу спавнов.
- [x] 3.2 Добавить в стенд размер экипажа: места из `CREW_ROLES.slice(0, crewSize)`, щит без места —
      от `nextShieldIntent`, только в фазе боя и до шага симуляции. Дописать комментарий в шапку
      `apps/server/src/rooms/shieldAutopilot.ts` о запрете рантайм-импортов.
- [x] 3.3 Добавить флаги `--crew`, `--enemy-offset` (слияние вглубь `enemySkill`), `--preset-id`,
      `--out`; сделать `--preset` необязательным с падением на путь пресета сервера, разрешаемый от
      `import.meta.url`.
- [x] 3.4 Завести `apps/server/scripts/run-balance-batch.mjs`: обход матрицы осей, запись двух
      документов атомарно, перезапись агрегатов после каждой ячейки, строка NDJSON о завершённой
      ячейке в stdout.
- [x] 3.5 Дополнить `apps/server/scripts/autopilot-stats.node-test.mjs`: сид по-прежнему повторяется
      бит в бит; за батч куплено больше одного `upgradeId`; экипаж 1 не падает на импорте автопилота
      щита. Проверка: `node --test apps/server/scripts/autopilot-stats.node-test.mjs`.

## 4. Схема отчёта

- [x] 4.1 Завести `packages/protocol/src/balanceStats.ts`: `BALANCE_STATS_FILE_VERSION = 1`, схемы
      заявки на батч, документа агрегатов и документа деталей, все `.strict()`. Реэкспорт из
      `index.ts`.
- [x] 4.2 Тесты схемы: отчёт другой версии не проходит разбор; частичная ячейка со статусом «идёт»
      проходит; заявка с пустой осью отклоняется. Проверка:
      `pnpm --filter @spaceship-defender/protocol test`.
- [x] 4.3 Привести запись стенда из 3.4 к схеме — стенд разбирает собственный документ перед
      записью. Проверка:
      `node apps/server/scripts/run-balance-batch.mjs --runs 2 --levels ace     --offsets 0 --crews 1,2,3 --out <tmp>`
      даёт документ, проходящий схему.

## 5. Сервер: хранилище, стенд-раннер, маршруты

- [x] 5.1 `apps/server/src/balanceStats/store.ts`: каталог из конфигурации (`STATS_BATCH_DIR`, по
      умолчанию рядом с пресетом), атомарная запись, чтение с отбрасыванием чужой версии и счётчиком
      отброшенных, пометка `stopped` для документа со статусом «идёт» при загрузке, ротация до
      `STATS_BATCH_KEEP`.
- [x] 5.2 `apps/server/src/balanceStats/runner.ts`: один батч на процесс, спавн со стражем
      `owned-process-guard.mjs`, разбор NDJSON прогресса, остановка по запросу, дедлайн, 503 при
      отсутствии файла стенда.
- [x] 5.3 `apps/server/src/balanceStats/routes.ts`: шесть маршрутов под `/admin/stats/*` за
      `isStatsRequestAuthorized` с паролем баланса, `no-store`, 409 на вторую заявку, 400 на
      превышение потолка. Регистрация в `apps/server/src/index.ts`.
- [x] 5.4 Тесты сервера: 401 без пароля не с петли, 409, 503, 400 на потолок, ротация, прерванный
      батч помечается `stopped`. Проверка: `pnpm --filter @spaceship-defender/server test`.
- [x] 5.5 Дописать `.env.example`: `STATS_BATCH_DIR`, `STATS_BATCH_KEEP`, потолок и дедлайн батча.

## 6. Консоль: вкладка, запуск, таблицы, лог

- [x] 6.1 `apps/admin/src/statsClient.ts` по образцу балансового клиента, ответы через схемы из
      `protocol`. Тест на 401 и на отказ при чужой версии.
- [x] 6.2 Зарегистрировать вкладку `stats` «Статистика» в реестре экранов и собрать
      `StatsScreen/index.tsx` из композиции без выборки данных внутри дочерних компонентов.
- [x] 6.3 `BatchLauncher.tsx`: выбор осей, число прогонов, первый сид, потолок волн, кнопка запуска,
      прогресс идущего батча, предупреждение о нагрузке на сервер.
- [x] 6.4 `BatchPicker.tsx` и `RunLog.tsx`: список батчей от новых к старым и таблица прогонов по
      образцу `.entries` из экрана волн.
- [x] 6.5 `aggregate.ts` с чистыми селекторами рядов и `aggregate.test.ts`. Проверка:
      `pnpm --filter @spaceship-defender/admin test`.

## 7. Графики и документация

- [x] 7.1 Примитивы `charts/BarChart.tsx`, `charts/LineChart.tsx`, `charts/HeatTable.tsx` и чистый
      `charts/scale.ts` с тестом на домены и деления.
- [x] 7.2 `OverviewSection.tsx`: волна по уровню бота, волна по сложности врага, карта ячеек, доля
      дошедших до волны, причина поражения.
- [x] 7.3 `CombatSection.tsx`: урон нанесённый и полученный по волнам, точность и перегрев по
      стволам, состав волны.
- [x] 7.4 `EconomySection.tsx`: кредиты заработано против потрачено по волнам и купленные апгрейды с
      расхождением голосов из детального отчёта.
- [x] 7.5 Стили `.chart*` в `apps/admin/src/styles.css` на существующих токенах; второй батч
      пунктиром, чтобы читалось без цвета.
- [x] 7.6 Строка про батчи статистики в таблицу команд `CLAUDE.md`; заодно поправить там две
      устаревшие константы — `PROTOCOL_VERSION` 35 и `BALANCE_FILE_VERSION` 24.
- [x] 7.7 Полный гейт. Проверка: `pnpm check` и `pnpm spec:validate` с явным перехватом кода
      возврата. Результат 2026-08-30: `format:check`, `lint`, `typecheck` зелёные; `pnpm -r test` —
      protocol 78/78, game-core 184/184, admin 54/54, client-shared 10/10, controller 69/69, server
      184/185; `build`, `smoke:network` и `test:e2e` (5/5, 2.6 мин) зелёные, `REST EXIT: 0`;
      `pnpm spec:validate` проходит. **Единственное падение — предсуществующее и не наше:**
      `SpaceshipDefenderRoom.test.ts` «publishes depletion and re-arms itself once the battery is
      back» даёт `energy: 5` вместо 0 одинаково при полностью застэшенном рабочем дереве, то есть
      это состояние ветки. Чинить отдельно, в рамках `shield-timing`.

## 7a. Экономика: атрибуция трат и схемы прокачки

- [x] 7a.1 Записывать покупку из передышки на волну, которая её оплатила (`stats-observer.mjs`):
      покупка резолвится между двумя боевыми окнами, поэтому по-волновой `creditsSpent` читался
      нулём и график экономики показывал доход против плоской линии. Проверка: батч на 16 прогонов —
      по-волновые траты 2:5 3:5 4:5 5:3.13 6:3.5 7:0.71, всего 330 кредитов при 993 заработанных.
- [x] 7a.2 Добавить в ячейку `builds` (схемы прокачки: набор апгрейдов, число прогонов, медианная и
      лучшая волна, медианные очки) и `upgradeImpact` (медиана волны с апгрейдом против без), в
      прогон — его схему. Схемы и вклад в `EconomySection`, схема прогона колонкой в `RunLog`.
      Проверка: `pnpm --filter @spaceship-defender/{protocol,admin} test`.

## 8. Ручная приёмка

- [ ] 8.1 Прогнать батч из консоли на текущем пресете по всем четырём осям и посмотреть глазами:
      читаются ли графики как единая панель, легка ли теплокарта на тёмной теме, удобен ли лог.
- [ ] 8.2 Свести правдоподобие: `DEMO_BOT_LEVEL=ace pnpm demo:visible` рядом с батчем на том же
      пресете — умирает ли headless-бот по тем же причинам, что и видимый.
- [ ] 8.3 Аудит смены свежим ревьюером по `$realtime-game-contract` (экономика и границы
      авторитета).
