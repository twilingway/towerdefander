# Tasks

## 1. Опорный замер до правок

- [ ] 1.1 `pnpm stats:autopilot --runs 16 --level veteran --seed 1 --crew 3 --enemy-offset 0` —
      медиана, среднее, max и медианная длина сюда. Кинетика после change обязана дать те же числа.
- [ ] 1.2 `pnpm benchmark:combat` — `pureFixedStep` p50/p95 сюда же.

## 2. Вид ствола и его числа

- [ ] 2.1 `FRIENDLY_WEAPON_KINDS` (`kinetic` | `laser` | `missile`) и поля `cannonWeaponKind`,
      `mgWeaponKind` в конфиге ядра и в balance-схеме; `BALANCE_FILE_VERSION` растёт, миграция
      аддитивная, по полям.
- [ ] 2.2 Новые статы в `SHIP_STAT_FIELDS`: `cannonLaserRange`, `mgLaserRange`, `laserBeamRadius`,
      `friendlyMissileTurnRatePerSecond`, `friendlyMissileAcquireConeRadians`. Умолчания подобраны
      так, чтобы кинетический пресет их не читал вовсе.
- [ ] 2.3 Таблица «стат → сценарий» в `shipStats.test.ts`: лазерные статы проверяются на лазерном
      пресете, ракетные на ракетном, остальные на кинетическом. Без неё половина гарантии из
      прошлого change проходит вхолостую.

## 3. Лазер

- [ ] 3.1 `WeaponOutcome` получает `beam`; `advanceFriendlyWeapon` ветвится по виду, нагрев и
      кулдаун остаются общими.
- [ ] 3.2 Разрешение луча в `resolveFriendlyHits` тем же кодом, что и снаряда: источник с началом в
      дульном срезе и концом на дальности. Проверка: тест на «бьёт ближнего из двух на оси», «дальше
      дальности — мимо» и «стреляет при забитом потолке снарядов».
- [ ] 3.3 `laserBeams` в состоянии боя и **в белом списке `pickCombatResult`**; живут два тика.
- [ ] 3.4 Провод: коллекция вспышек под `@view(1)`, `PROTOCOL_VERSION` растёт, оба roomView.
- [ ] 3.5 Дисплей рисует луч.

## 4. Ракета

- [ ] 4.1 Необязательные поля наведения в `ProjectileState`; выбор цели один раз при пуске среди
      врагов в конусе захвата.
- [ ] 4.2 Ветка наведения в `moveProjectiles` по образцу `moveMissile`. Проверка: тест на
      «доворачивает к сместившейся цели» и «потеряв цель, летит прямо и не берёт новую».
- [ ] 4.3 Дисплей рисует дружественную ракету отличимо от пули.

## 5. Консоль

- [ ] 5.1 Вид ствола и его параметры на вкладке «Игрок», рядом с остальными числами ствола;
      подсказка называет цену каждого вида.

## 6. Проверка

- [ ] 6.1 Повторить замер 1.1 на кинетическом пресете: числа обязаны совпасть.
- [ ] 6.2 Три пресета на одном корпусе, те же сиды: медианы обязаны лечь в один диапазон. Записать
      сюда все три и точность по слотам.
- [ ] 6.3 `pnpm benchmark:combat` — луч не должен удлинить шаг, ракета не должна заметно.
- [ ] 6.4 `pnpm check` и `pnpm spec:validate` с явным перехватом кода возврата.

## 7. Ручная приёмка

- [ ] 7.1 `pnpm demo:visible` на лазерном пресете: видно ли, куда ушёл импульс и во что попал.
- [ ] 7.2 То же на ракетном: догоняют ли ракеты и не выглядит ли это автоприцелом.
