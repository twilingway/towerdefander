# Tasks

## 1. Контракт

- [x] 1.1 `bossEscortShare` в `campaignAuthoringSchema`, `BALANCE_FILE_VERSION` вверх, прошлая
      версия в списке мигрируемых.
- [x] 1.2 Дефолт доли в `DEFAULT_CAMPAIGN_AUTHORING` вместе с новыми `budgetBase`, `budgetGrowth` и
      `groupStartStepSeconds`.
- [x] 1.3 Регрессионный тест: документ версии 33 грузится с сохранённой таблицей волн. Проверено на
      мутанте — миграция, переносящая блок авторинга целиком, валит его.

## 2. Генератор

- [x] 2.1 `buildWave` определяет босса до состава и заполняет волну до
      `бюджет − стоимость     босса` с полом по доле эскорта.
- [x] 2.2 Новые числа бюджета и шага групп в `FALLBACK_AUTHORING`.
- [x] 2.3 Кампания пересобрана: пресет и `authoredCampaign.ts` одной командой.

## 3. Консоль

- [x] 3.1 Поле доли эскорта на вкладке «Волны».

## 4. Стенд

- [x] 4.0 Сетевой смоук развязан от кампании: своя таблица из трёх маленьких волн кладётся через
      админский маршрут перед созданием комнаты. Причина измерена, не угадана — зонд показал
      `phase result`, hp 0 на тике 1065 и одиннадцать живых врагов при трёх убитых, а правки экипажа
      (уклонение, липкая цель, срок удержания) подняли стенд только до одного падения из трёх.
      Экипажные правки оставлены: они делают стенд устойчивее и сами по себе.

## 5. Замер

- [ ] 4.1 Батч теми же аргументами, что базовый `batch-1bcbf77c`: медианы обязаны съехать с кратных
      пяти.
- [ ] 4.2 `pnpm benchmark:combat` — плотность выросла, шаг обязан остаться в бюджете.
- [ ] 5.3 Глазами оператора: волна читается как толпа, а не как очередь.
