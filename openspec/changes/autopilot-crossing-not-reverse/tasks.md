# Tasks

## 1. Перелёт носом вперёд

- [x] 1.1 Помечать перелётом курс ветки без цели и запрещать на нём задний ход.
- [x] 1.2 Тесты: перелёт назад даёт доворот без тяги, а тот же курс при цели остаётся реверсом. Оба
      обязаны краснеть до правки.

## 2. Догадка внутри арены

- [x] 2.1 Зажимать запомненную позицию стрелка на круг арены и считать курс от самой позиции.
- [x] 2.2 Тест: выстрел у края даёт догадку не дальше границы, и она отрабатывается по прибытии.

## 3. Проверка

- [x] 3.1 `node --test apps/controller/scripts/visible-demo-policy.node-test.mjs`.
- [x] 3.2 Замер стендом до и после на 40 прогонах: доля тиков задним ходом, доля у края арены и
      разбивка заднего хода по манёврам.
- [ ] 3.3 `pnpm check` с явным перехватом кода возврата и `pnpm spec:validate`.
- [ ] 3.4 Приёмка глазами: `pnpm demo:visible` — бот не пятится от края через пол-карты.
