## 1. Учёт рекордов

- [x] 1.1 `apps/server/src/stats/records.ts`: `ServerRecords` с картой открытых комнат, рекордами
      людей и комнат за всё время и пиками по суткам в поясе сервера; `observe(metadata)`,
      `forget(statsId)`, срез для страницы с пиками за 30 дней. Проверка: unit-тесты — новый рекорд,
      падение ниже рекорда, пик внутри дня, смена суток, забытая комната, повторный `forget`.
- [x] 1.2 Файл рекордов: ручная проверка документа версии 1, чтение при старте, `.unusable` для
      нечитаемого или чужого документа, запись через временный файл и `rename`, не больше одной
      записи сразу и слияние изменений. Проверка: unit-тесты на временном каталоге — перечитанный
      файл даёт те же рекорды, мусор уходит в `.unusable`, ошибка записи не бросает и повторяется
      при следующем росте.
- [x] 1.3 `apps/server/src/config.ts`: `STATS_RECORDS_PATH` с умолчанием
      `apps/server/data/stats-records.json`; `.env.example`. Проверка: тест конфигурации.

## 2. Комнаты сообщают о себе

- [x] 2.1 Кампания: `queueMetadataUpdate` отдаёт метаданные учёту, `onDispose` забывает комнату.
      Проверка: тест комнаты кампании.
- [x] 2.2 Арена: `publishStats` отдаёт метаданные учёту, новый `onDispose` забывает комнату.
      Проверка: тест комнаты арены.
- [x] 2.3 `apps/server/src/index.ts`: загрузка учёта при старте рядом с пресетом, учёт в маршруты
      статистики. Проверка: `pnpm typecheck`.

## 3. Страница

- [x] 3.1 `/stats/rooms.json`: поле `records`. Проверка: `stats.test.ts` — поле есть, 401 его не
      отдаёт, в сериализованном ответе нет персональных данных.
- [x] 3.2 `/stats/rooms`: блок «Рекорды» с датами и таблица «Пики по дням» с поясом в заголовке.
      Проверка: тест разметки в `stats.test.ts`.

## 4. Выкатка

- [x] 4.1 `docker-compose.prod.yml`: `STATS_RECORDS_PATH: /data/stats-records.json` рядом с
      `BALANCE_PRESET_PATH`. Проверка: `git diff` показывает строку в окружении `space-api`.

## 5. Проверка

- [x] 5.1 `pnpm check` с перехватом кода возврата; `pnpm spec:validate`.
- [x] 5.2 На стенде разработчика: соло-забег поднимает рекорд до одного человека и одной комнаты на
      `/stats/rooms`, а файл рекордов появляется в `apps/server/data`.
