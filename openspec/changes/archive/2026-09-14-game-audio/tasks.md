# Tasks

## 1. Файлы и каталог

- [x] 1.1 Файлы в `apps/display/public/audio/sounds/` и `apps/display/public/audio/music/` под
      латинскими именами; README рядом объясняет, что и откуда. Проверка: файлы в репозитории.
- [x] 1.2 `packages/protocol/src/audioCatalogue.ts`: id звуков и тем плюс русские названия, по
      образцу `effectCatalogue.ts`. Проверка: схема отвергает id, которого нет в каталоге.
- [x] 1.3 Тест дисплея: у каждого id каталога есть файл на диске. Проверка: тест падает, если id
      добавлен без файла.

## 2. Слоты в пресете

- [x] 2.1 `shipSoundsSchema` у корпуса (выстрел пушки, выстрел пулемёта, попадание, смерть) и
      `enemySoundsSchema` у визуала врага (выстрел, попадание, смерть), оба необязательные целиком.
      `BALANCE_FILE_VERSION` поднят, миграция переносит документ без слотов.
- [x] 2.2 Пресет → конфиг: звуки корпуса в `SpaceshipSimulationConfig` рядом с `shipMuzzleEffect`.
- [x] 2.3 Обе комнаты публикуют звуки: корпус — в `display`, архетипы — в записи каталога врагов.
      `PROTOCOL_VERSION` поднят, адаптеры вида комнаты обновлены.

## 3. Звуковой автобус дисплея

- [x] 3.1 Чистая часть: громкости, Mute, потолок копий, затухание по расстоянию — модуль без Web
      Audio, с тестами.
- [x] 3.2 Драйвер на Web Audio: загрузка и декодирование, две шины через `GainNode`, снятие
      `suspended` по первому жесту.
- [x] 3.3 Настройки в `localStorage`, чтение при старте, запись при изменении.

## 4. Пульт и темы

- [x] 4.1 Кнопка настроек на экране игры и панель: два ползунка и Mute.
- [x] 4.2 Тема вне боя и тема в бою, зациклено, без наложения.

## 5. Сцена подаёт голос

- [x] 5.1 Выстрелы: пушка и пулемёт своего корабля и чужих корпусов на арене.
- [x] 5.2 Попадания и гибель: враги в кампании, корпуса на арене, отдельный звук боса.

## 6. Консоль

- [x] 6.1 Селекторы звука в карточке корпуса.
- [x] 6.2 Селекторы звука в карточке архетипа врага.
