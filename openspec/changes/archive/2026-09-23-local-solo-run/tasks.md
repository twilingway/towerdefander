# Tasks

## 1. Пакет: то, что сегодня умеет только комната

- [x] 1.1 `src/localMirror.ts` — дерево простых объектов, удовлетворяющее и цели проекции, и
      структурному типу, который читает адаптер вида.
- [x] 1.2 `src/enemyCatalogue.ts` — публикация каталога врагов и визуалов корпуса из конфигурации,
      вынесенная из комнаты; комната зовёт её же.
- [x] 1.3 `src/waveDeadline.ts` — дедлайн волны в тиках: старт, продление на салвадж, истечение.
- [x] 1.4 Тесты пакета: зеркало принимается проекцией, дедлайн считается в тиках.

## 2. Устройство: прогон

- [x] 2.1 `model/localRun/clock.ts` — аккумулятор с потолком догона.
- [x] 2.2 `model/localRun/engine.ts` — конфигурация из затравки, создание прогона, шаг с автопилотом
      незанятых мест, дедлайн волны, переходы фаз.
- [x] 2.3 `model/localRun/publish.ts` — проекция в зеркало, `toDisplayRoomView`, три приёмника в том
      же порядке, что у комнаты.
- [x] 2.4 `model/localRun/driver.ts` — привод сцены: шаг внутри кадра, который его рисует.
- [x] 2.5 `model/hooks/useLocalRun.ts` — жизненный цикл, пауза на скрытой вкладке, запасной кадр,
      пока сцена не поднялась.

## 3. Экран

- [x] 3.1 `screens/RoomScreen/LocalRunRoute.tsx` и запись `/solo` в таблице маршрутов.
- [x] 3.2 `RoomScreen` получает проп `cockpit` и перестаёт спрашивать про сессию в десяти местах.
- [x] 3.3 Плитка «На этом устройстве» ведёт в `/solo`; `?online` оставляет прежний путь через
      комнату.
- [x] 3.4 Локальный рекорд на экране результата, хранится на устройстве.

## 4. Проверка

- [x] 4.1 Unit: арифметика дедлайна и потолок догона аккумулятора.
- [x] 4.2 `tests/e2e/local-solo.spec.ts` с заблокированным origin сервера: волна идёт, корабль
      слушается клавиатуры.
- [x] 4.3 `pnpm check` с явным перехватом кода возврата и `pnpm spec:validate`.
- [x] 4.4 Приёмка оператора: отклик в локальном прогоне против серверного, на телефоне.

## 5. Симуляция в воркере

- [x] 5.1 `model/localRun/runWorker.ts` и `workerProtocol.ts`: прогон, часы шагов и сборка вида в
      воркере; положение корабля после каждого шага, вид с частотой патча.
- [x] 5.2 `model/localRun/hosts.ts`: хозяин во вкладке и хозяин-воркер с общим интерфейсом;
      `?worker=0` оставляет прежний путь; сторож называет замолчавший воркер.
- [x] 5.3 Замер на Redmi 4X: 37.1 / 36.8 к/с с воркером против 33.9 / 32.4 без него.
- [x] 5.4 Пять боёв подряд на телефоне: по одному воркеру в бою, ноль после выхода; куча страницы
      после сборки мусора 20.1–21.0 МБ.
