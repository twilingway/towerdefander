# Задачи

## 1. Хранилище мира

- [x] 1.1 `apps/display/src/model/worldStore.ts`: изменяемый снимок, счётчик версии, набор
      слушателей, `publishWorld`; кэш среза вынесен в `createSliceCache` и покрыт тестами — в этом
      пакете нет DOM, и хук иначе не проверить.
- [x] 1.2 Патч пишет в хранилище на каждом патче, а не на медленных часах страницы.
- [x] 1.3 `useWorldSlice(select, equals)` поверх `useSyncExternalStore`.

## 2. Корень перестаёт перерисовываться

- [x] 2.1 `needsRootRender`: в бою корень спит, вне боя следует за патчем — передышку и итог рисует
      он сам.
- [x] 2.2 Боевые панели переехали в `apps/display/src/screens/BattleScreen/panels.tsx`, каждая со
      своим сравнением.
- [x] 2.3 Атрибуты арены (`data-spaceship-*`) пишутся в узел напрямую раз в 100 мс: они — окно
      тестов и демо-бота в мир, а страница в бою больше не перерисовывается.

## 3. Разбор снимка

- [x] 3.1 Полная проверка схемой — на смену «момента» (комната, фаза, прогон, наличие мира), а не на
      каждый патч. Разбор упал с 30.5 до 4.0 мс/с.

## 4. Отрисовка (добавлено по профилю, а не по плану)

Профиль через CDP (`scripts/profile-display.mjs`) показал две трети потока в графическом конвейере
Phaser и ничего нашего в первых двадцати восьми строках. `Graphics` и фигуры вроде `add.circle`
обходятся и триангулируются заново на каждом кадре, где они видны.

- [x] 4.1 Пол арены, обод, кольца и спицы — одна текстура 2048×2048, растянутая до мировых размеров.
- [x] 4.2 Корпус, носовая метка и ствол — изображения, как враги уже давно.
- [x] 4.3 Камень без пресетной модели — изображение вместо двух фигур.

## 5. Приёмка

- [x] 5.1 `node scripts/bench-panels.mjs --cpu=4`: 122 к/с и 25.5 % рывков до, **157.5 к/с, средний
      кадр 6.1 мс, худший 6.0 мс, рывков 0.0 %** после. Эталонный стенд на тех же условиях — 165
      к/с, 6.1 мс, 6.0 мс, 0 %.
- [x] 5.2 `pnpm check` целиком зелёный.
