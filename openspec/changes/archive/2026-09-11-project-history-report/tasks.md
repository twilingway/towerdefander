## 1. Свидетельства дня

- [x] 1.1 Добавить скрипт, который по дате собирает коммиты с телами, изменённые пути, diffstat,
      смену версий протокола и балансовых файлов и пишет `evidence.json` и `evidence.md`.
- [x] 1.2 Связать день с OpenSpec-изменениями по затронутым путям и не приписывать связь там, где её
      нет.

## 2. Съёмка дня

- [x] 2.1 Поднять стенд выбранной ревизии в detached worktree: установка зависимостей с офлайновым
      приоритетом, сервер и vite-приложения на выделенном блоке портов.
- [x] 2.2 Описать рецепты эпох и снять по ним лобби, пульт, консоль баланса и бой; неснятый кадр
      пометить причиной в `capture-status.md`.
- [x] 2.3 Сделать прогон возобновляемым и убрать worktree после дня, а при неудачной уборке сообщить
      её путь.

## 3. Отчёт

- [x] 3.1 Написать описание каждого дня на русском: что было накануне, что появилось, зачем это
      игроку, что при этом сломали и починили.
- [x] 3.2 Собрать `HISTORY.md`, манифест и самодостаточную HTML-страницу с таймлайном эпох и
      кадрами; вписаться в предельный вес страницы.

## 4. Локальный дашборд

- [x] 4.1 Добавить статический дашборд и локальный сервер к нему по образцу ежедневника
      видеоотчётов.

## 5. Проверка

- [x] 5.1 Снять первый, средний и последний день и убедиться, что кадры принадлежат своим версиям.
- [x] 5.2 Прогнать `pnpm format`, `pnpm lint`, `pnpm typecheck`, `pnpm test` и `pnpm spec:validate`.
