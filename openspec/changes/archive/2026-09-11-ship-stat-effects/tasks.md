# Tasks

## 1. Опорный замер до правок

- [x] 1.1 `pnpm stats:autopilot --runs 16 --level veteran --seed 1 --crew 3 --enemy-offset 0`: волна
      min 5, медиана 7, среднее 6.4, max 9; очки медиана 1176, среднее 970.9; длина медиана 453.4 с.
      После change эти числа обязаны совпасть: change ничего не балансирует, любое расхождение —
      регрессия.
- [x] 1.2 `pnpm benchmark:combat` до правок: `pureFixedStep` p50 `0.1402`, p95 `0.3329`.

## 2. Тест на два конфига — написан первым и до конца переноса красный

- [x] 2.1 Сценарий в `packages/game-core/src/shipStats.test.ts`: четыре живучих врага вокруг
      корабля, тяга вперёд, накат и реверс, оба ствола бьют по прицелу, который перебрасывается на
      противоположный борт, щит держится до полного разряда, а во второй половине запрашивается и
      снимается быстрее, чем успевает подняться. Трасса — состояние после 600 шагов.
- [x] 2.2 Для каждого из 41 поля: шаг с удвоенным полем в конфиге обязан дать ту же трассу, а
      удвоение того же поля в `state.ship` — другую. Сообщение при падении называет, какая половина
      упала. При первом запуске красными были все 41.
- [x] 2.3 Исключений нет: сценарий доводит каждое поле до того, чтобы оно на что-то влияло. Три поля
      потребовали доработки сценария — `spaceshipMaxHp` (нужен подобранный ремонт на побитом
      корпусе), `shieldMinimumUpTicks` и `shieldCooldownTicks` (нужен запрос щита короче времени
      подъёма).

## 3. Поля, правила и форма эффекта

- [x] 3.1 `SHIP_STAT_FIELDS` (41 имя) и `MODULE_TARGET_FIELDS` (минус `shieldRadius` и
      `headingAngularBrakingPerSecondSquared`) в `packages/game-core/src/shipStats.ts`. Список живёт
      в ядре, а не в протоколе: ядро ни от чего не зависит, а тип статов нужен именно ему.
- [x] 3.2 `PLAYER_SHIP_FIELDS` в мигрaторе оставлен на месте и сверен с новым списком: у него на три
      имени больше — визуалы ствола, пули и носовой пули, которые числами не являются. Расхождение
      ловит тест миграции с версии 24, который заполняет весь блок из умолчаний.
- [x] 3.3 `ShipStatEffect` и code-owned `SHIP_STAT_RULES`: кулдауны целыми тиками и не ниже четверти
      базы, тики щита целыми, дуга не больше `2π`, реверс не больше единицы, всё остальное не ниже
      нуля.
- [x] 3.4 `computeShipStats(base, effects)` по формуле из design. Восемь тестов: суммы, проценты,
      множители, независимость от порядка, каждое правило.

## 4. Статы в состоянии

- [x] 4.1 `ship: ShipStats` и `purchasedUpgrades` в `CombatStateFields`, оба в белом списке
      `pickCombatResult`. `roleModifiers` и отдельный `spaceshipMaxHp` удалены: максимум корпуса
      больше не хранится дважды.
- [x] 4.2 Пересчёт на покупке в `applyUpgrade`: от базы прогона и всего купленного, правило корпуса
      `Δ = next − prev`, подрезание `shieldEnergy` упавшей ёмкостью.

## 5. Перенос чтений в симуляции

- [x] 5.1 `spaceshipSimulation.ts` — 40 чтений.
- [x] 5.2 `collisions.ts` (радиус щита и дуга) и `simulationMath.ts` (радиус корпуса, срок жизни
      снаряда).
- [x] 5.3 `simulationCombatBridge.ts` (9 чтений), `loot.ts`, `combat.ts`.
- [x] 5.4 `moveSpaceshipWithinWorld` и `removeExpiredProjectiles` получили статы параметром;
      `isInsideShieldArc` и `rechargedForCombat` наоборот потеряли ставший ненужным конфиг.
      Публичный API пакета не изменился.
- [x] 5.5 Тест из задачи 2 зелёный целиком, список исключений пуст.

## 6. Каталог как данные

- [x] 6.1 Девять апгрейдов переписаны в эффекты по таблице из design; `apply`-замыкания и типы
      `RoleModifiers` удалены. Проверка — не отдельный тест, а замер задачи 8.1: прогон по тем же
      сидам совпал до последнего числа, включая очки и длину.
- [x] 6.2 Подпись карты остаётся написанной руками: композитор из эффектов и таблица единиц
      переезжают в change про дерево, когда модули начнёт писать оператор. Обоснование в design.

## 7. Провод и клиенты

- [x] 7.1 `RoleModifiersState` (девять `float32` на клиента на тик) и четыре схемы модификаторов
      удалены, `PROTOCOL_VERSION` 37 → 38, оба roomView-адаптера и оба preview-режима вычищены.
- [x] 7.2 `purchasedUpgrades` публикуется в display-блоке как список строк — меняется раз в волну.
      Отладочная строка `CrewLatency` показывает купленное вместо трёх множителей.
- [x] 7.3 Проекция берёт ёмкость и дугу щита, ёмкости нагрева и максимум корпуса из состояния.
      Попутно исчезло расхождение: дуга щита теперь одна и та же для блокирования и для отрисовки.
- [x] 7.4 `upgrade-votes.mjs`, `balance-run.mjs`, `stats-observer.mjs` читают статы;
      `network-smoke.mjs` проверяет покупку по опубликованному списку купленного.

## 8. Проверка

- [x] 8.1 Тот же стенд, те же сиды: волна min 5, медиана 7, среднее 6.4, max 9; очки медиана 1176,
      среднее 970.9; длина медиана 453.4 с. **Совпало с задачей 1.1 до последнего числа** — прогон
      по сиду не сдвинулся ни на тик.
- [x] 8.2 `pnpm benchmark:combat`: `pureFixedStep` p50 `0.1396`, p95 `0.3455`,
      `pureFixedStepP95AtMost2Ms` истинно. Пересчёт раз в волну в шаге не виден.
- [x] 8.3 `pnpm format:check` чисто, `pnpm lint` код 0, `pnpm typecheck` код 0; protocol 78/78,
      game-core 250/250, display 122/122, controller 76/76 плюс 69 node-тестов политики, admin
      59/59, client-shared 10/10, server 185/186. `pnpm build` код 0, `pnpm smoke:network` код 0,
      `pnpm test:e2e` 5 passed за 2.6 мин код 0, `pnpm demo:verify` пройден целиком,
      `openspec validate ship-stat-effects --strict` — valid. Единственное падение —
      предсуществующее `SpaceshipDefenderRoom.test.ts` «publishes depletion and re-arms itself»
      (`energy: 5` вместо 0, чинится в `shield-timing`).
