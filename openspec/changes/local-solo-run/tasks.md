# Tasks

## 1. Пакет: то, что сегодня умеет только комната

- [ ] 1.1 `src/localMirror.ts` — дерево простых объектов, удовлетворяющее и цели проекции, и
      структурному типу, который читает адаптер вида.
- [ ] 1.2 `src/enemyCatalogue.ts` — публикация каталога врагов и визуалов корпуса из конфигурации,
      вынесенная из комнаты; комната зовёт её же.
- [ ] 1.3 `src/waveDeadline.ts` — дедлайн волны в тиках: старт, продление на салвадж, истечение.
- [ ] 1.4 Тесты пакета: зеркало принимается проекцией, дедлайн считается в тиках.

## 2. Устройство: прогон

- [ ] 2.1 `model/localRun/clock.ts` — аккумулятор с потолком догона.
- [ ] 2.2 `model/localRun/engine.ts` — конфигурация из затравки, создание прогона, шаг с автопилотом
      незанятых мест, дедлайн волны, переходы фаз.
- [ ] 2.3 `model/localRun/publish.ts` — проекция в зеркало, `toDisplayRoomView`, три приёмника в том
      же порядке, что у комнаты.
- [ ] 2.4 `model/localRun/driver.ts` — привод сцены: шаг внутри кадра, который его рисует.
- [ ] 2.5 `model/hooks/useLocalRun.ts` — жизненный цикл, пауза на скрытой вкладке, запасной кадр,
      пока сцена не поднялась.

## 3. Экран

- [ ] 3.1 `screens/RoomScreen/LocalRunRoute.tsx` и запись `/solo` в таблице маршрутов.
- [ ] 3.2 `RoomScreen` получает проп `cockpit` и перестаёт спрашивать про сессию в десяти местах.
- [ ] 3.3 Плитка «На этом устройстве» ведёт в `/solo`; `?online` оставляет прежний путь через
      комнату.
- [ ] 3.4 Локальный рекорд на экране результата, хранится на устройстве.

## 4. Проверка

- [ ] 4.1 Unit: арифметика дедлайна и потолок догона аккумулятора.
- [ ] 4.2 `tests/e2e/local-solo.spec.ts` с заблокированным origin сервера: волна идёт, корабль
      слушается клавиатуры.
- [ ] 4.3 `pnpm check` с явным перехватом кода возврата и `pnpm spec:validate`.
- [ ] 4.4 Приёмка оператора: отклик в локальном прогоне против серверного, на телефоне.
