# Tasks

## 1. Измеритель

- [x] 1.1 Инструмент, снимающий на живом развёртывании темп тиков сервера, распределение интервалов
      прихода и время замершей картинки против бюджета дисплея. Проверка: прогон по loopback и через
      публичный адрес даёт разные цифры на одном и том же сервере.

## 2. Запас

- [ ] 2.1 Оценка разброса рядом с существующей оценкой интервала, тем же сглаживанием. Проверка: на
      ровной последовательности оценка мала, на последовательности с выбросами растёт.
- [ ] 2.2 Запас считается из оценки разброса, с полом, потолком и разной скоростью роста и спада.
      Проверка: тесты на ровный канал, на выбросы, на возвращение и на потолок.
- [ ] 2.3 Часы воспроизведения используют вычисленный запас вместо константы. Проверка: существующие
      тесты часов остаются зелёными.

## 3. Проверка

- [ ] 3.1 Замер на публичном адресе до и после: время замершей картинки за одинаковый отрезок.
- [ ] 3.2 Приёмка глазами на телефоне: у края арены корабль идёт без пауз.
- [ ] 3.3 `pnpm check` и `pnpm spec:validate`.
