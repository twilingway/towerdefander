## 1. Схема пресета

- [x] 1.1 Добавить `autopilotProfileSchema`, `autopilotLevelSchema` и `autopilotTuningSchema` в
      `packages/protocol/src/balance.ts`, подключить полем `autopilot` в `balanceTuningSchema` рядом
      с презентационными полями; поднять `BALANCE_FILE_VERSION` до 10 и дописать 9 в
      `LEGACY_BALANCE_FILE_VERSIONS`.
- [x] 1.2 Покрыть тестами границы каждого поля профиля, обязательность всех трёх профилей и отказ
      `.strict()` на лишнем ключе; проверить
      `pnpm --filter @spaceship-defender/protocol exec vitest run src/balance.test.ts`.

## 2. Сервер

- [x] 2.1 Добавить дефолты трёх уровней и перечислить `autopilot` в `createDefaultTuning()`;
      отбросить раздел в `toSimulationConfig`, чтобы он не попадал в конфиг симуляции.
- [x] 2.2 Написать миграцию v9 → v10 по образцу `migratePlayerShip`: уровень и каждый профиль
      заполняются из дефолтов; дописать строку в док-комментарий миграций.
- [x] 2.3 Покрыть тестами: документ версии 9 мигрирует и продолжает играться теми же числами,
      повторная миграция ничего не меняет, текущая версия возвращается нетронутой; сначала
      убедиться, что тест краснеет без миграции. Проверить
      `pnpm --filter @spaceship-defender/server exec vitest run src/balance/balance.test.ts`.

## 3. Дисплей

- [x] 3.1 Вынести фильтр кадра камеры в отдельную функцию и переиспользовать её существующим выбором
      цели и угрозы.
- [x] 3.2 Написать чистый сборщик картины мира: корабль, щит, пулемёт, башня и четыре коллекции
      сущностей внутри кадра.
- [x] 3.3 Публиковать картину в demo-режиме через свойство окна; при обычном адресе публикации быть
      не должно.
- [x] 3.4 Покрыть тестами фильтрацию за кадром, сохранность полей внутри кадра и пустой мир;
      проверить `pnpm --filter @spaceship-defender/display exec vitest run src/visibleDemo.test.ts`.

## 4. Политика автопилота

- [x] 4.1 Добавить экстраполяцию сущностей на прошедшее время и сеяный генератор разброса прицела.
- [x] 4.2 Написать ранжирование целей и угроз: приоритет ракет, босса, стреляющих врагов, добиваемых
      целей и астероидов на курсе.
- [x] 4.3 Написать `planGunner`: упреждение по доле профиля, конус башни, разброс прицела.
- [x] 4.4 Написать `planShield`: предсказание контакта с радиусом перехвата, опережение из профиля,
      порог энергии, явное выключение против защёлки.
- [x] 4.5 Написать `planPilot` со строгим приоритетом: уход, дистанция с обходом, наведение носом,
      нулевой вектор; конус пулемёта и потолок нагрева.
- [x] 4.6 Покрыть тестами каждый пункт так, чтобы тест краснел без реализации: цель вне конуса не
      даёт огня, ракета внутри горизонта разворачивает вектор поперёк пеленга, разряженный щит
      получает выключение, нулевая доля упреждения целится в текущую точку. Проверить
      `pnpm --filter @spaceship-defender/controller test`.

## 5. Харнесс демонстрации

- [x] 5.1 Читать профиль активного уровня по HTTP при старте; отказ запроса завершать понятной
      ошибкой, а не молчаливыми зашитыми числами.
- [x] 5.2 Поддержать перекрытие уровня переменной окружения и прокинуть её вместе с адресом баланса
      из корневого раннера.
- [x] 5.3 Заменить отправку команд вызовами трёх планировщиков, читать картину мира в опросе
      телеметрии, восстанавливать интенты при возврате в бой после антракта.
- [x] 5.4 Показать активный уровень в оверлее демонстрации и закрепить проверочный прогон на уровне
      `rookie`.

## 6. Консоль баланса

- [x] 6.1 Добавить вкладку «Автопилот» в `TABS` и `TAB_LABELS`.
- [x] 6.2 Написать экран автопилота: выбор активного уровня и три карточки профилей; углы в
      градусах, времена в секундах, умения переключателями.
- [x] 6.3 Дописать глоссарий вкладки в том же стиле, что у экранов врагов и игрока; проверить
      `pnpm --filter @spaceship-defender/admin exec vitest run`.

## 7. Проверка

- [x] 7.1 `pnpm check` и `pnpm spec:validate`; отчитаться, что прогнано и что пропущено с причиной.
- [x] 7.2 `pnpm demo:verify` отдельным прогоном вне общего гейта.
- [ ] 7.4 Приёмка глазами: сравнить один и тот же пресет на уровнях `rookie` и `ace` в
      `pnpm demo:visible` — держит ли `ace` дистанцию, молчит ли пулемётом вне конуса, поднимает ли
      щит под ракету.
- [x] 7.5 Ограничить кольцо боя кадром камеры и погасить в консоли ручки выключенных умений; тесты
      должны краснеть без ограничения. Проверить
      `pnpm --filter @spaceship-defender/controller test`.
