# Tasks

## 1. Регрессии

- [x] 1.1 Записать в `packages/game-core/src/circularCombat.test.ts` три случая: прижатый к обода
      враг продолжает идти вдоль стены; вырожденная тангенциальная геометрия разлипает не позже
      порога; вдали от обода траектория не изменилась. Проверка: первые два падают на неправленом
      `moveEnemy`, третий проходит.

## 2. Разлипание

- [x] 2.1 Перенести направление облёта из чётности номера появления во внутреннее поле врага и
      задавать его при спавне; на провод поле не идёт.
- [x] 2.2 Учесть близость врага к ободу в смеси хода: гасить отступление и отдавать освободившуюся
      долю скорости облёту.
- [x] 2.3 Разворачивать направление облёта, когда после ограничения арены от хода остаётся меньше
      фиксированной доли паспортной скорости, а встречное направление даёт больше.

## 3. Проверка

- [x] 3.1 `pnpm --filter @spaceship-defender/game-core exec vitest run src/circularCombat.test.ts` и
      соседние наборы ядра — зелено.
- [x] 3.2 `pnpm check` и `pnpm spec:validate`.
- [x] 3.3 `pnpm benchmark:combat` — p95 чистого шага остаётся в бюджете 2 ms.
- [x] 3.4 `pnpm stats:autopilot --runs 20` до и после правки: медиана достигнутой волны не падает.
- [ ] 3.5 Приёмка глазами: `pnpm demo:visible`, подлететь вплотную к врагу у края и увидеть, что он
      уходит вдоль стены.
