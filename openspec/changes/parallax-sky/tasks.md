## 1. Формулы слоёв

- [ ] 1.1 `viewport.ts`: `NEBULA_PARALLAX`; запас картинки от скорости с полом 1.18 и потолком 1.6
      кадра; сдвиг туманности со скоростью; `STAR_PARALLAX` под три слоя
- [ ] 1.2 Юнит-тесты: туманность идёт с постоянной долей хода камеры и не выходит за запас; запас не
      меньше пола и не больше потолка; каждый ближний слой быстрее дальнего, а туманность медленнее
      всех; сила 0 прибивает небо к экрану

Проверка:
`pnpm --filter @spaceship-defender/display exec vitest run src/game/spaceshipViewModel.test.ts`.

## 2. Слои в сцене

- [ ] 2.1 `backdrop.ts`: три `Blitter` с запечённой точкой своего размера, сид и мерцание прежние;
      поле из 170 звёзд убрано
- [ ] 2.2 Размер и сдвиг картинки по 1.1; пересчёт раскладки при смене окна и масштаба

Проверка: `pnpm --filter @spaceship-defender/display exec vitest run src/game`, `pnpm typecheck`.

## 3. Консоль

- [ ] 3.1 Подсказка к силе параллакса описывает слои

Проверка: `pnpm --filter @spaceship-defender/admin exec vitest run`.

## 4. Приёмка

- [ ] 4.1 Скорости, числа звёзд и размеры точек подобраны на стенде по полёту; итог вписан в
      design.md
- [ ] 4.2 `node scripts/bench-panels.mjs --cpu=4 --plain --wave=5` на новом небе против базы из
      design.md
- [ ] 4.3 `pnpm check` с перехватом кода возврата; `pnpm spec:validate`
