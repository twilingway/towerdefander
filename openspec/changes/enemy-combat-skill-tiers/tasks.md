# Tasks

## 1. Схема и миграция (делается первой, иначе теряются пресеты)

- [x] 1.1 Поднять `BALANCE_FILE_VERSION` 20 → 21 и добавить 20 в `LEGACY_BALANCE_FILE_VERSIONS`
      (`packages/protocol/src/balance.ts`). Проверка:
      `pnpm --filter @spaceship-defender/protocol     test` до добавления полей — существующие тесты
      версии зелёные.
- [x] 1.2 Завести `enemySkillLevelSchema`, `enemySkillProfileSchema` (десять ручек, у каждой
      min/max) и `enemySkillTuningSchema` (`offset` −2..+2 целый плюс три профиля), включить в
      `balanceTuningSchema`. Схемы `.strict()`, как соседние.
- [x] 1.3 Добавить `combatSkill` в `enemyArchetypeSchema` и обновить фикстуру архетипа в
      `packages/protocol/src/balance.test.ts`.
- [x] 1.4 Дополнить `migrateArchetype` в `apps/server/src/balance/store.ts` заполнением
      `combatSkill` по цепочке `сохранённое ?? дефолт этого id ?? константа`.
- [x] 1.5 Написать `migrateEnemySkill` по образцу `migrateAutopilot`: слияние **по полям внутри
      каждого уровня**, отсутствующий уровень берётся из дефолтов целиком.
- [x] 1.6 Регрессия в `apps/server/src/balance/balance.test.ts` по образцу теста про устаревший
      профиль автопилота: документ версии 20 с ручной кампанией грузится, кампания цела, `warn` не
      вызывался. Проверка: тест падает, если убрать 1.5.

## 2. Профиль в ядре

- [x] 2.1 Отзеркалить типы профиля в `packages/game-core/src/combat.ts`, добавить
      `CombatConfig.enemySkill` и проверки диапазонов в `validateCombatConfig` рядом с остальными.
- [x] 2.2 Задать дефолтные профили и `combatSkill` пяти встроенным архетипам в
      `packages/game-core/src/spaceshipSimulation.ts`; нейтральные значения ручек обязаны
      воспроизводить сегодняшнее поведение.
- [x] 2.3 Написать `resolveEnemySkill(level, offset)` — насыщающее разрешение уровня, чистая
      функция. Проверка: юнит-тест на сдвиг за оба края списка.
- [x] 2.4 Завести врагу внутренние поля: запомненная позиция и скорость корабля с тиком снимка,
      состояние потока разброса. На провод не идут; заполнить их при спавне и во всех литералах
      состояния врага в тестах и бенчмарке.

## 3. Поведение

- [x] 3.1 Провести `moveEnemy` через профиль: полоса смешивания и доля облёта из ручек, дистанция
      боя растёт на `retreatStandoffFactor` ниже `retreatHpFraction`. Разлипание у обода не трогать.
- [x] 3.2 Добавить восприятие с задержкой `reactionTicks` и экстраполяцию запомненного между
      снимками.
- [x] 3.3 Добавить расталкивание и разведение по секторам. Проверка: тест на рой из одного архетипа
      — ни одна пара не ближе суммы радиусов, пеленги различны.
- [x] 3.4 Добавить уклонение от дружественных снарядов в горизонте `evadeHorizonTicks`.
- [x] 3.5 Провести проход стрельбы через упреждение и разброс из seeded потока (`deriveDomainSeed` +
      `nextUint32`). Проверка: тест на попадание при ровном боковом ходе цели и тест на побитовое
      совпадение двух прогонов по одному seed.

## 4. Консоль

- [ ] 4.1 Селект `combatSkill` на карточке врага рядом с селектом появления, с русскими подписями.
- [ ] 4.2 Вкладка профилей мастерства по образцу вкладки автопилота: три карточки уровней в
      операторских единицах (градусы, секунды, проценты) плюс поле сдвига сложности.
- [ ] 4.3 Записи о новых полях в легенду каталога врагов и значение `combatSkill` в литерал нового
      архетипа `createArchetype`.
- [ ] 4.4 Обновить фикстуру архетипа в `apps/admin/src/waveSummary.test.ts` и в
      `apps/controller/scripts/visible-demo-policy.node-test.mjs`.

## 5. Раскладка и замер

- [ ] 5.1 Снять базовую линию до раскладки:
      `pnpm stats:autopilot --runs 40 --seed 4242 --preset     data/balance.json --json` (путь
      резолвится от `apps/server`).
- [ ] 5.2 Назначить уровни пяти встроенным архетипам и тем же в `apps/server/data/balance.json`,
      исходя из того, что `veteran` заметно злее нынешнего врага, а `rookie` слабее.
- [ ] 5.3 Повторить замер и убедиться, что медиана достигнутой волны падает при росте
      `enemySkill.offset` — иначе ползунок ни на что не влияет и раскладку надо переделывать.

## 6. Проверка

- [ ] 6.1 `pnpm check` и `pnpm spec:validate`.
- [ ] 6.2 `pnpm benchmark:combat` — p95 чистого шага остаётся в бюджете 2 ms при 196 сущностях.
- [ ] 6.3 Приёмка глазами: `pnpm demo:visible` — рой не слипается в точку, стрелки берут упреждение,
      подранки отходят.
