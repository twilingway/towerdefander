# Tasks

## 1. Протокол

- [x] 1.1 Добавить в команду пилота необязательные доворот и тягу в `[-1, 1]`, поднять
      `PROTOCOL_VERSION` 30 → 31.
- [x] 1.2 Тесты протокола: команда со стика без новых полей проходит, значения вне диапазона
      отклоняются. `pnpm --filter @spaceship-defender/protocol exec vitest run src/index.test.ts`

## 2. Ядро

- [x] 2.1 Провести доворот и тягу до `applyPilotInput`: при довороте запрошенный курс пустеет, а
      намерение сохраняется во вводе пилота.
- [x] 2.2 Вести угловой ход корпуса по запрошенной угловой скорости, оставив прежние ускорение и
      торможение; нулевой доворот гасит вращение.
- [x] 2.3 Считать целевую скорость вдоль носа по тяге, включая отрицательную.
- [x] 2.4 Тесты ядра: тап-доворот не возвращает нос, реверс не крутит нос, команда без доворота
      ведёт себя как раньше. `pnpm --filter @spaceship-defender/game-core exec vitest run`

## 3. Комната

- [x] 3.1 Передавать доворот и тягу из команды в ядро, сохранив проверку последовательности и
      таймаут ввода.
- [x] 3.2 Тест комнаты: намерение доходит до состояния, а команда без него не меняет поведения.

## 4. Контроллер

- [x] 4.1 Клавиатурный руль шлёт доворот и тягу; удалить предсказание точки остановки, стоп-тики и
      опережение курса из танковой ветки.
- [x] 4.2 Стик и схема `absolute` продолжают слать вектор без намерения.
- [x] 4.3 Тесты панели и руля.
      `pnpm --filter @spaceship-defender/controller exec vitest run src/pilotKeyboard.test.ts`

## 5. Проверка

- [x] 5.1 Обновить `tests/e2e/pilot-helm.spec.ts` и `tests/e2e/solo-room.spec.ts` под доворот и
      реверс; ассерты не ослаблять.
- [x] 5.2 `pnpm check > log 2>&1; echo "EXIT:$?"` и `pnpm spec:validate`.
- [ ] 5.3 Приёмка глазами: быстрые тапы по A и по D не уводят нос в противоположную сторону, W и S
      идут вдоль носа.
