# Tasks

## 1. Точность наведения

- [x] 1.1 Нести вперёд угол башни и курс корпуса в картине мира по скорости, измеренной между
      соседними сырыми кадрами; ограничивать паспортной скоростью башни.
- [x] 1.2 Поднять частоту телеметрии до частоты команд.
- [x] 1.3 Считать скорость вращения пеленга из относительной скорости вместо двух выборок.
- [x] 1.4 Учесть время доворота в упреждении — для башни и для носового пулемёта.
- [x] 1.5 Тесты на всё перечисленное; каждый обязан краснеть до правки.

## 2. Безголовый стенд статистики

- [x] 2.1 Перевести `packages/game-core` на `.ts`-спецификаторы, иначе пакет не запускается из
      исходников обычным Node.
- [x] 2.2 Собрать картину мира для политики прямо из состояния симуляции, в той же форме, что даёт
      дисплей, — включая обрезку по кадру камеры.
- [x] 2.3 Прогонять цикл «решение — применение — шаг» и голосовать за улучшения, иначе кредиты
      копятся и меряется корабль, на котором никто не летает.
- [x] 2.4 Печатать сводку и подробности по прогонам; уровень, сид, пресет и пауза задаются
      аргументами.
- [x] 2.5 Тест на воспроизводимость: один сид даёт побитово одинаковый вывод дважды.

## 3. Проверка

- [x] 3.1 `pnpm check` с явным перехватом кода возврата и `pnpm spec:validate`.
- [ ] 3.2 Приёмка глазами: `DEMO_BOT_LEVEL=ace pnpm demo:visible` — снаряды накрывают цель, а не
      проходят рядом.

> 3.1 частично: все стадии гейта для этого изменения проходят по отдельности — формат, линт по
> `apps packages tests`, типы, тесты, сборка, сетевой дым и e2e. Сам `pnpm check` падает на
> `eslint .`, который подхватывает `.daily-worktrees/` (в `.gitignore`) и отслеживаемый
> `tools/daily-video-dashboard/app.js`: шесть ошибок разбора в файлах вне этого изменения. Чинится
> отдельно.

## 4. Находки стенда

- [ ] 4.1 Разобраться, почему уровни автопилота статистически неразличимы. На 40 прогонах каждого
      медиана достигнутой волны одинакова (6), а по счёту **новичок выигрывает** — 1527 против 1077
      у ветерана и 1182 у аса. Пушка не имеет ни нагрева, ни боезапаса, поэтому дисциплина огня у
      аса — чистый убыток: он теряет выстрелы, ничего не получая взамен. Градация станет
      осмысленной, только если у стрельбы появится цена.
