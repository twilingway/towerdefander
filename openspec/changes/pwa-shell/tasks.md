## 1. Сборка и манифест

- [ ] 1.1 Добавить `vite-plugin-pwa` в `apps/display` и собрать `generateSW` с манифестом,
      `registerType: "prompt"`, precache без музыки и runtime-кэшем музыки. Проверка: `pnpm build`,
      в `apps/display/dist` есть `sw.js` и `manifest.webmanifest`, в списке precache нет
      `.mp3`/`.ogg`.
- [ ] 1.2 Иконки 192, 512 и maskable из утверждённой оператором картинки, ссылки в `index.html`.
      Проверка: манифест в DevTools без ошибок на боевой сборке через `vite preview`.

## 2. Регистрация, обновление, сброс

- [ ] 2.1 Регистрация только при `import.meta.env.PROD`, до неё — разбор `?sw=off` (снять
      регистрации, стереть кэши, не регистрировать). Проверка: unit-тест разбора флага; на
      `pnpm dev` у страницы нет service worker'а.
- [ ] 2.2 Стор обновления: `onNeedRefresh` ставит флаг, `updateSW` за кнопкой. Проверка: unit-тест
      стора.
- [ ] 2.3 Предложение «Есть новая версия — обновить» на стартовом экране и в лобби, и нигде в бою.
      Проверка: компонентный тест — в бою не рендерится.

## 3. Хост

- [ ] 3.1 `docker/nginx-static.conf`: `sw.js` и манифест с `Cache-Control: no-cache`. Проверка:
      `docker build` образа web и `curl -I` на `sw.js` и на файл из `/assets/`.

## 4. Проверки

- [ ] 4.1 e2e на боевой сборке (`vite preview` на своём порту): открыть при сети, выключить сеть
      (`context.setOffline`), перезагрузить — стартовый экран и локальный прогон работают.
- [ ] 4.2 e2e: вторая сборка на том же порту — предложение обновиться появляется в лобби, не в бою;
      после нажатия подвал показывает новую сборку.
- [ ] 4.3 Аварийная сборка `selfDestroying` на предпросмотре: после неё воркера и кэшей нет.
- [ ] 4.4 `pnpm check`, `pnpm spec:validate`.

## 5. Приёмка

- [ ] 5.1 Приёмка оператора: установка на Redmi 4X и Note 10 Pro, запуск без сети, обновление после
      релиза.
