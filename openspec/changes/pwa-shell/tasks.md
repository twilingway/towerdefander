## 1. Сборка и манифест

- [x] 1.1 Добавить `vite-plugin-pwa` в `apps/display` и собрать `generateSW` с манифестом,
      `registerType: "prompt"`, precache без музыки, музыка мимо воркера. Проверка: `pnpm build`, в
      `apps/display/dist` есть `sw.js` и `manifest.webmanifest`, в списке precache нет
      `.mp3`/`.ogg`.
- [x] 1.2 Иконки 192, 512 и maskable из утверждённой оператором картинки, ссылки в `index.html`.
      Проверка: `pnpm pwa:check` — манифест найден и разбирается без ошибок (`Page.getAppManifest`,
      то же, что показывает DevTools).

## 2. Регистрация, обновление, сброс

- [x] 2.1 Регистрация только при `import.meta.env.PROD`, до неё — разбор `?sw=off` (снять
      регистрации, стереть кэши, не регистрировать). Проверка: unit-тест разбора флага; e2e
      `local-solo.spec.ts` — на dev-сервере у страницы нет service worker'а.
- [x] 2.2 Стор обновления: `onNeedRefresh` ставит флаг, `updateSW` за кнопкой. Проверка: unit-тест
      стора.
- [x] 2.3 Предложение «Есть новая версия — обновить» на стартовом экране и в лобби, и нигде в бою.
      Проверка: `pnpm pwa:check` — новая сборка посреди боя не перезагружает страницу и не
      предлагается, на стартовом экране предлагается.

## 3. Хост

- [ ] 3.1 `docker/nginx-static.conf`: `sw.js` и манифест с `Cache-Control: no-cache`. Проверка:
      после релиза `curl -I` на проде — `sw.js` и манифест с `no-cache`, файл из `/assets/` с
      `immutable`. Решено оператором 23.09.2026: отдельного предпрода под одну эту проверку не
      поднимать; Chrome и без заголовка сверяет `sw.js` мимо своего HTTP-кэша.

## 4. Проверки

- [x] 4.1 `pnpm pwa:check` (`scripts/check-pwa.mjs`): боевая сборка на своём порту 36480, воркер
      регистрируется и кэширует игру; без сети (`context.setOffline`) стартовый экран и локальный
      прогон работают. Вне `pnpm check`: дисплей собирается четыре раза.
- [x] 4.2 Там же: вторая сборка на том же порту не перезагружает страницу в бою и не предлагается
      там; на стартовом экране предлагается, после нажатия подвал показывает новую сборку.
- [x] 4.3 Там же: `?sw=off` и аварийная сборка `PWA_SELF_DESTROY=1` оставляют происхождение без
      воркера и без кэшей; в конце в `dist` собирается обычная сборка.
- [x] 4.4 `pnpm check`, `pnpm spec:validate`.

## 5. Приёмка

- [ ] 5.1 Приёмка оператора: установка на Redmi 4X и Note 10 Pro, запуск без сети, обновление после
      релиза.
