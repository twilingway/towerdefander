# Tasks

## 1. Манёвренность в схеме и дефолтах

- [x] 1.1 Добавить в `enemyArchetypeSchema` поля `turnRatePerSecond`,
      `turnAccelerationPerSecondSquared`, `turnBrakingPerSecondSquared`; схема `.strict()`, поэтому
      без правки любой документ с новым полем отвергается.
- [x] 1.2 Задать значения пяти встроенным архетипам так, чтобы босс разворачивался заметно тяжелее
      перехватчика, и включить новые поля в проверку `validateCombatConfig`.
- [x] 1.3 Поднять `BALANCE_FILE_VERSION` и дополнить `migrateArchetype`, чтобы сохранённый пресет
      прошлой версии поднимался со значениями из дефолтов, а не падал.
- [x] 1.4 Показать поля в каталоге врагов админки в единицах, понятных оператору.

## 2. Инерция поворота врага

- [x] 2.1 Экспортировать `advanceAngularTraverse` и завести врагу внутреннее поле угловой скорости;
      на провод оно не идёт.
- [x] 2.2 Провести курс врага через инерцию вместо присваивания из `atan2`.
- [x] 2.3 Смешивать радиальную и тангенциальную составляющие непрерывно, без скачка на границе
      дистанции боя.
- [x] 2.4 Тест: разворот врага за тик не превышает паспортного, и тест обязан краснеть на текущем
      коде. Отдельно — что враг на дистанции боя не переворачивается взад-вперёд.

## 3. Разрыв патовой карусели

- [x] 3.1 Передать скорость башни из пресета в политику через `options`, как уже сделано для
      скоростей снарядов.
- [x] 3.2 Считать требуемую угловую скорость пеленга и распознавать непобедимую геометрию.
- [x] 3.3 При непобедимой геометрии идти курсом на точку встречи вместо кольца.
- [x] 3.4 Тест: цель уходит по касательной с равной скоростью — бот обязан сменить манёвр.

## 4. Живучесть в рою

- [x] 4.1 Не менять цель, пока башня едет к текущему пеленгу, если новая не лучше с большим отрывом.
- [x] 4.2 Открывать огонь, когда ошибка наведения меньше того, что башня проходит за тик.
- [x] 4.3 Щит как резерв: против неизбежного попадания подниматься независимо от порога энергии и не
      падать, пока в окне есть следующее попадание.
- [x] 4.4 Считать огонь из пулемёта от реального курса во время уклонения.
- [x] 4.5 Тесты на всё перечисленное; каждый обязан краснеть до правки.

## 5. Проверка

- [ ] 5.1 `pnpm check` с явным перехватом кода возврата и `pnpm spec:validate`.
- [x] 5.2 Проверить, что сохранённый пресет прошлой версии поднимается после миграции.
- [ ] 5.3 Приёмка глазами: `DEMO_BOT_LEVEL=ace pnpm demo:visible` — карусель разрывается, враги на
      дистанции боя не дёргаются, ас в рою стреляет и держит щит.

> 5.1 частично: все стадии гейта для этого изменения проходят по отдельности — формат, линт по
> `apps packages tests`, типы, тесты, сборка, сетевой дым и e2e. Сам `pnpm check` падает на
> `eslint .`, который подхватывает `.daily-worktrees/` и `tools/daily-video-dashboard/`: шесть
> ошибок парсинга в файлах вне этого изменения и вне git. Чинится отдельно.
