## 1. Таблица архетипов в pure core

- [x] 1.1 Ввести `EnemyWeaponTuning` и `EnemyArchetype`, заменить поля `gunship*`/`carrier*`/
      `hostileBullet*`/`missile*` в `CombatConfig` на `enemyArchetypes`, перенести дефолты без
      изменения чисел; проверить `pnpm --filter @spaceship-defender/game-core exec vitest run`.
- [x] 1.2 Перенести `damage`, `shieldHitCost` и `turnRatePerSecond` на инстансы hostile bullet и
      homing missile, читать их при разрешении щита и урона вместо ветвления по типу; проверить
      тесты щита и коллизий.
- [x] 1.3 Убрать все ветвления по enemy kind в `moveEnemy`, блоке стрельбы, `spawnEntity` и
      начислении score/credits в пользу чтения архетипа; проверить, что тесты детерминизма и наград
      проходят без правки ожиданий.

## 2. Кампания волн

- [x] 2.1 Ввести `WaveSpawnEntry`, `WaveDefinition`, `DirectorTuning`, `WaveCampaign` и
      `SpawnSector` с дефолтом «пустая таблица + нынешние ручки директора»; проверить, что состав
      процедурных волн не изменился.
- [x] 2.2 Построить план явной волны из записей таблицы без обращения к spawn RNG за составом,
      добавить поэлементный `spawnIntervalTicks` и сектор в `PendingSpawn` и учесть их при спавне;
      покрыть тестами явный состав, поэлементный дрип и ограничение угла сектором.
- [x] 2.3 Читать `spawnCost` и `unlockWave` из архетипов в процедурном директоре и учитывать явные
      переопределения множителей HP и темпа в `getWaveDifficulty`; переписать тест разблокировки
      carrier на конфигурируемую волну.

## 3. Три новых типа врагов

- [x] 3.1 Добавить `sniper`, `interceptor`, `boss` в `EnemyKind` и `ENEMY_KINDS` с baseline-числами
      архетипов, поднять `PROTOCOL_VERSION` до 15; проверить тесты protocol и game-core.
- [x] 3.2 Реализовать залповый огонь по `burstCount`/`burstSpreadRadians` в пределах caps и
      `bossWaveInterval` в директоре; покрыть тестами один cooldown на залп, отсутствие burst сразу
      после освобождения cap и появление boss на настроенной волне.
- [x] 3.3 Прогнать `pnpm benchmark:combat` и зафиксировать время шага против reference; при просадке
      скорректировать `spawnCost` boss и sniper, не трогая caps.

## 4. Схемы баланса в protocol

- [x] 4.1 Создать `packages/protocol/src/balance.ts` со схемами архетипа, оружия, записи волны,
      кампании, пресета и файла пресетов, реэкспортировать из `index.ts`.
- [x] 4.2 Покрыть тестами отклонение неположительного HP, `unlockWave` меньше единицы, неизвестного
      enemy kind, частичного набора архетипов и caps выше протокольных.

## 5. Сервер: пресеты и admin API

- [x] 5.1 Добавить `BALANCE_PRESET_PATH` и `ADMIN_BALANCE_PASSWORD` в `apps/server/src/config.ts` и
      `.env.example`; проверить тесты конфигурации.
- [x] 5.2 Реализовать `apps/server/src/balance/store.ts`: чтение файла, разбор схемой, кэш,
      атомарная запись через временный файл и rename, работа на defaults при отсутствующем либо
      повреждённом файле; покрыть тестами оба отказных случая.
- [x] 5.3 Реализовать `apps/server/src/balance/routes.ts` с `GET /admin/balance`,
      `GET /admin/balance/defaults`, `POST /admin/balance/validate`, `PUT /admin/balance`,
      авторизацией через существующий loopback/Basic-хелпер и заголовками `no-store`; покрыть
      тестами 401 без пароля не с loopback, 400 на невалидный пресет и успешное сохранение.
- [x] 5.4 Применять активный пресет в `tryStartRun` и не трогать идущий run; покрыть тестом, что
      следующий run стартует на новом балансе, а текущий продолжает на прежнем.

## 6. Дисплей

- [x] 6.1 Заменить ветвление силуэтов на полную таблицу `ENEMY_VISUALS` по `EnemyKind` и
      масштабировать геометрию от authoritative радиуса; проверить типы и тесты display.
- [x] 6.2 Добавить HP-бар над boss на основе `hp`/`maxHp` из снапшота; проверить тесты display.

## 7. Консоль баланса

- [x] 7.1 Завести приложение `apps/admin` (package.json, vite config с proxy `/admin` на сервер,
      index.html, main.tsx, styles.css) на порту 5175 и добавить его в корневой скрипт `dev`;
      проверить `pnpm --filter @spaceship-defender/admin build`.
- [x] 7.2 Реализовать экран архетипов и экран директора с валидацией схемами protocol и показом
      серверной ошибки без потери введённых значений.
- [x] 7.3 Реализовать экран таблицы волн с составом, интервалами, секторами, переопределениями и
      сводкой стоимости волны против бюджета директора; caps показать только для чтения.
- [x] 7.4 Реализовать экран пресетов: список, переключение активного, копия, экспорт и импорт JSON.

## 8. Проверка

- [x] 8.1 Прогнать `pnpm check` и `pnpm spec:validate`, устранить расхождения.
- [ ] 8.2 Провести ручной прогон: изменить состав волны 1 и HP `interceptor` в консоли, запустить
      run и убедиться, что волна пришла заданным составом, а на дисплее различимы пять силуэтов.
- [ ] 8.3 Провести read-only reviewer-аудит изменений и закрыть findings уровня blocker/high/medium.

## 9. Фаза босса

- [x] 9.1 Исключить boss из перемешивания состава и дописывать его в хвост плана директора; покрыть
      тестом, что boss стоит последним при нескольких сидах.
- [x] 9.2 Задерживать спавн kind с политикой «boss», пока жива хотя бы одна wave-угроза, не считая
      ambient asteroids; покрыть тестами удержание, освобождение после зачистки, безразличие к
      ambient asteroid и то, что волна не уходит в intermission с боссом в очереди.
- [x] 9.3 Вывести `spawnPolicy` на экран врагов в консоли баланса с пояснением побочного эффекта;
      проверить `pnpm --filter @spaceship-defender/admin build`.

## 10. Каталог врагов и мультисекторы

- [x] 10.1 Заменить фиксированный `EnemyKind` идентификатором каталога в протоколе и ядре, перенести
      силуэт и цвет в данные архетипа, поднять `PROTOCOL_VERSION` до 16.
- [x] 10.2 Заменить один сектор записи волны набором секторов и выбирать сектор seeded-потоком;
      покрыть тестами распределение по отмеченным секторам.
- [x] 10.3 Публиковать каталог визуалов в display projection и рисовать врагов по форме из данных с
      запасным силуэтом для неизвестной формы.
- [x] 10.4 Мигрировать файл пресетов версии 1 в версию 2, сохранив состав волн и добавив визуалы;
      покрыть тестом.
- [x] 10.5 Дать в консоли конструктор врагов (копия, удаление с защитой от используемых,
      переименование, форма, цвета, HP-бар), чипы секторов и пояснения к полям.
- [x] 10.6 Заменить одно орудие архетипа списком орудий с независимыми cooldown, мигрировать файл
      пресетов версии 2 и дать в консоли добавление и удаление орудий.
- [x] 10.7 Дать переопределение множителей HP и темпа на каждую группу волны, показывать в таблице
      расчётные HP и перезарядку, задавать сектор по умолчанию в новой записи.
- [x] 10.8 Перевести настройки времени в консоли из тиков в секунды, сузить числовые колонки,
      добавить кнопку создания врага с нуля и легенду параметров.
- [x] 10.9 Отделить масштаб модели от радиуса поражения, показать в консоли превью силуэта с зоной
      поражения и эталоном корабля.
