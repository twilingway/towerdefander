# Tasks

## 1. Прогноз ракеты

- [x] 1.1 Считать время и точку выхода ракеты на кольцо щита полётом с паспортной скоростью
      поворота; снаряды и астероиды оставить на пересечении прямой.
- [ ] 1.2 Взять тот же прогноз для порога уклонения, чтобы решение об уходе не мигало вместе с тем,
      попала ракета на прямую в этот тик или нет.
- [ ] 1.3 Тесты: ракета, выпущенная в сторону от корабля, даёт контакт и пеленг; она же поднимает
      вектор ухода. Оба обязаны краснеть до правки.

## 2. Доворот на ожидаемую угрозу

- [x] 2.1 Разворачивать сектор щита на ближайшего врага в кадре, когда в воздухе ничего нет, и на
      запомненную позицию выстрела, когда кадр пуст.
- [x] 2.2 Оставить поднятие щита за окном упреждения и запасом энергии из профиля.
- [x] 2.3 Тесты: враг в кадре разворачивает сектор и не поднимает щит; пустой кадр с памятью о
      выстреле разворачивает сектор туда же.

## 3. Босс против своих ракет

- [ ] 3.1 Ранжировать ракету выше прочего только внутри окна в две секунды, пока на поле есть босс.
- [ ] 3.2 Тесты: босс против ракеты в трёх секундах и в одной; ракета без босса на поле.

## 4. Проверка

- [ ] 4.1 `node --test apps/controller/scripts/visible-demo-policy.node-test.mjs`, затем
      `pnpm check` с явным перехватом кода возврата и `pnpm spec:validate`.
- [ ] 4.2 Замер стендом до и после:
      `pnpm stats:autopilot -- --runs 40 --level ace --preset     data/balance.json`, отдельно со
      `--start-wave 5`. Ожидание: средняя достигнутая волна аса выше пяти, здоровье босса на конце
      боя ниже.
- [ ] 4.3 Приёмка глазами: `DEMO_START_WAVE=5 DEMO_BOT_LEVEL=ace pnpm demo:visible` — башня бьёт
      босса, а не карусель его ракет, и сектор щита стоит в сторону врага до выстрела.
