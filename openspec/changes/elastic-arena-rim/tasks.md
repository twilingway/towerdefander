# Tasks

## 1. Ядро

- [ ] 1.1 Упругая полоса у края: чистая функция, считающая направленное внутрь ускорение по глубине
      захода и скорости наружу.
- [ ] 1.2 Применять её к скорости корабля в его шаге, до проекции на окружность.
- [ ] 1.3 Тесты: внутри арены полоса ничего не меняет, заход гасится за несколько tick без нулевого
      шага, более быстрый заход отталкивает сильнее, корабль остаётся внутри.
      `pnpm --filter @spaceship-defender/game-core exec vitest run`

## 2. Проверка

- [ ] 2.1 `pnpm check > log 2>&1; echo "EXIT:$?"` и `pnpm spec:validate`.
- [ ] 2.2 Повторный замер у края: шаг за tick перестаёт быть нулевым.
- [ ] 2.3 Приёмка глазами: у края картинка не замирает.
