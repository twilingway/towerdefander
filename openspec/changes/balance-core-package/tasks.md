# Tasks

## 1. Пакет

- [x] 1.1 Завести `packages/balance-core` с `package.json`, `tsconfig.json` и входами `.` и
      `./seed`, по образцу `packages/client-shared`.
- [x] 1.2 Перенести дефолты настройки (`createDefaultTuning`, `createDefaultPresetsFile`, профили
      оружия, дефолты автопилота и руля) в `src/defaults.ts`.
- [x] 1.3 Перенести `toSimulationConfig`, `resolveShipArchetype`, `findActivePreset` и
      `assertTuningIsPlayable` в `src/simulationConfig.ts`.
- [x] 1.4 Перенести `migrations.ts` и `shipCatalogue.ts` без правок логики.
- [x] 1.5 Собрать `src/loadBalanceDocument.ts`: миграция, разбор схемой, проверка играбельности,
      результат вместо исключения.
- [x] 1.6 Вынести отображение настройки в публичный каталог корпусов в `src/publicCatalogue.ts`.

## 2. Сервер

- [x] 2.1 Оставить в `balance/store.ts` файловый слой и реэкспорт переехавшего; проверить, что
      импорты комнаты, маршрутов и тестов не правятся.
- [x] 2.2 Перевести `BalanceStore.load` на `loadBalanceDocument`, сохранив поведение с непригодным
      файлом.
- [x] 2.3 Перевести обработчик `/ships` на `toPublicShipCatalogue`.
- [x] 2.4 Убрать собственную сборку конфигурации из `scripts/balance-run.mjs` и звать
      `toSimulationConfig`.

## 3. Затравка

- [x] 3.1 Перенести `production.json` и `production.revision` в `packages/balance-core/presets/` и
      отдать их входом `./seed`.
- [x] 3.2 Обновить путь в `scripts/promote-balance-seed.mjs`, `scripts/deploy-production.sh` и
      `scripts/export-balance-seed.sh`.
- [x] 3.3 Продвижение пишет ещё и `src/seedRevision.ts` — ту же ревизию модулем, который читает
      сборщик клиента; только для затравки самого пакета, не для `--out` в чужой каталог.
- [x] 3.4 Обновить пути в `docs/DEPLOYMENT.md` и `CLAUDE.md`.

## 4. Тесты

- [x] 4.1 Тест пакета: закоммиченная затравка читается тем же путём, что и на устройстве
      (`loadBalanceSeed`), мигрирует до текущей версии и проходит проверку играбельности.
- [x] 4.2 Тест пакета: документ, не принятый схемой, и документ с непригодными числами отвергаются с
      причиной. Миграция настоящего старого документа остаётся за `balance.test.ts` сервера —
      сегодняшние дефолты со штампом старой версии ничего не доказывают.
- [x] 4.3 Тест продвижения: текстовая ревизия и генерируемый модуль совпадают.
- [x] 4.4 Существующие тесты сервера (`balance.test.ts`, `rooms/balanceApplication.test.ts`)
      проходят без правок логики — поправлен только путь к затравке.

## 5. Проверка

- [x] 5.1 `pnpm check` зелёный: единственное падение — известный флейк `solo-room.spec.ts`,
      перезапуск `pnpm test:e2e` дал 10 passed / 1 skipped с явным перехватом кода возврата.
- [x] 5.2 `pnpm spec:validate` зелёный.
- [x] 5.3 Доказано, что удаление копии не сдвигает измеряемую игру: старая и новая сборка
      конфигурации сравнены на 64 ячейках (два пресета x все корпуса x четыре сдвига сложности).
      Разошлись ровно семь полей — `shieldBandEffect`, `shieldImpactEffect`, `shipDeathEffect`,
      `shipMuzzleEffect`, `shipCannonSound`, `shipMgSound`, `shipDeathSound`, — и все они строковые
      и симуляцией не читаются: старая копия просто теряла эффекты и звуки корпуса. Прогон
      `stats:batch` здесь ничего не добавил бы, потому что сравнение конфигураций сильнее выборки.
- [ ] 5.4 Приёмка оператора: прогон на стенде идёт на тех же числах, что и до переноса. Отложено
      оператором 23.09.2026: проверять после механизма прокачки корабля.
