# Tasks

## 1. Общая кинематика

- [ ] 1.1 Выделить кинематику корабля в чистую функцию `game-core`: позиция, скорость, курс, угловые
      скорости, цели и башня. Комната зовёт её внутри своего шага. Проверка: поведение не
      изменилось.
      `pnpm --filter @spaceship-defender/game-core exec vitest run src/spaceshipSimulation.test.ts`
- [ ] 1.2 Тест на детерминизм: одна и та же последовательность ввода из одного состояния даёт
      одинаковый результат дважды, и переворот одного кадра результат меняет.

## 2. Провод

- [ ] 2.1 Числа привода в проекции: линейные скорость, ускорение, торможение, множитель заднего
      хода. Проверка: значение в снимке совпадает с тем, что комната применяет после покупки модуля.
- [ ] 2.2 Переносимое состояние в проекции: угловые скорости корпуса и башни и их цели.
- [ ] 2.3 Подтверждённый номер ввода на поток. Поднять `PROTOCOL_VERSION`; оба адаптера `roomView`.
      `pnpm --filter @spaceship-defender/protocol exec vitest run src/index.test.ts`
- [ ] 2.4 Тест: набор полей, которые читает и пишет шаг, совпадает с набором, который едет на
      провод.

## 3. Ввод соло

- [ ] 3.1 Поток ввода соло через `defineInput` с монотонным номером и диапазонами; экипажные
      сообщения не тронуты. Проверка: повтор и устаревший кадр состояние не двигают.
      `pnpm --filter @spaceship-defender/server exec vitest run src/rooms/SpaceshipDefenderRoom.test.ts`
- [ ] 3.2 Номер поднимается только при действительно применённом вводе и обнуляется на новом
      прогоне.

## 4. Предсказание и интерполяция

- [ ] 4.1 Reconciler на своём корабле: набор полей равен набору шага, сведение расхождения, порог
      расхождения под наши величины.
- [ ] 4.2 Интерполяция чужих: позиции и углы разными вложениями, углы — по кратчайшей дуге.
- [ ] 4.3 Снаряды считаются вперёд по своей скорости; ракеты и враги — нет.
- [ ] 4.4 Задержку воспроизведения по-прежнему считает наш оценщик разброса и отдаёт её в SDK.
      Проверка: его тесты остаются зелёными без правки ожиданий.

## 5. Кадр дисплея

- [ ] 5.1 Сцена читает предсказанные и интерполированные значения напрямую; порядок кадра соблюдён —
      шаг, отправка ровно стольких кадров ввода, потом чтение для отрисовки.
- [ ] 5.2 Патч перестаёт публиковать новый объект вида в React на каждом тике. Проверка: прибор
      React на панели падает с 70 мс/с до единиц.
- [ ] 5.3 Исключение из правила границ записано в `docs/CODE_STYLE.md` вместе с причиной.
- [ ] 5.4 Убрать предсказание двух углов: его работу забирает reconciler.

## 6. Проверка

- [ ] 6.1 `pnpm check` с явным перехватом кода возврата и `pnpm spec:validate`.
- [ ] 6.2 Замер на телефоне тем же набором приборов: рваные кадры, React мс/с, кадры в секунду —
      против записи «до».
- [ ] 6.3 Приёмка глазами: корабль отзывается на палец, тумблер предсказания даёт видимую разницу,
      при потере пакетов корабль не дёргается.
