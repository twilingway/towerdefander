# Tasks

## 1. Дисплей

- [x] 1.1 Ввести буфер авторитетных снимков по gameplay tick и локальное время воспроизведения с
      адаптивным отставанием в `SpaceshipRuntime`, вместо фиксированного 50-мс окна.
- [x] 1.2 Перевести корабль, нос, башню, щит и боевые визуалы на этот общий таймлайн, сохранив snap
      на первом снимке, hydration и reconnect.
- [x] 1.3 Тесты хелперов таймлайна: ровный темп 62 мс не даёт паузы, патч с двумя шагами
      проигрывается отрезком, патч без нового тика не перезапускает переход.
      `pnpm --filter @spaceship-defender/display exec vitest run src/game/spaceshipViewModel.test.ts`

## 2. Сервер

- [x] 2.1 Перевести цикл комнаты на `setSimulationInterval` с аккумулятором реального времени и
      потолком догона.
- [x] 2.2 Тест комнаты: при грубом таймере за отрезок реального времени выполняется ожидаемое число
      шагов, а одиночная долгая задержка не даёт лавину.
      `pnpm --filter @spaceship-defender/server exec vitest run src/rooms/SpaceshipDefenderRoom.test.ts`

## 3. Проверка

- [x] 3.1 `pnpm check` с явным перехватом кода возврата и `pnpm spec:validate`.
- [x] 3.2 Повторный замер темпа: интервал прихода патчей остаётся прежним, но число шагов за секунду
      становится 20, а не 16.
- [ ] 3.3 Приёмка глазами: у края арены корабль идёт без пауз.
