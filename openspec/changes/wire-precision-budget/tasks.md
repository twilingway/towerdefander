# Tasks

## 1. Сужение полей

- [ ] 1.1 Снять базовую линию: `pnpm benchmark:combat`, записать `patchBytes` и `timingsMs`.
- [ ] 1.2 Перевести все `float64` в публикуемом состоянии на `float32`.
- [ ] 1.3 Поднять `PROTOCOL_VERSION`.

## 2. Правило на будущее

- [ ] 2.1 Записать в соглашения проекта правило выбора ширины поля в проводе, с ориентиром на
      наблюдаемую точность и явным запретом восьмибайтовых величин боя.

## 3. Проверка

- [ ] 3.1 Повторить `pnpm benchmark:combat` и сравнить с базовой линией: `patchBytes.movingEntities`
      падает, `timingsMs.schemaSync` не растёт.
- [ ] 3.2 `pnpm check` и `pnpm spec:validate`.
