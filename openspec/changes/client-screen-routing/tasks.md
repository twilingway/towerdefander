# Tasks

Каждая группа — отдельный набор коммитов и собственный гейт. Порядок обязателен: правило
переписывается до установки зависимости, а самый рискованный файл режется последним.

## 1. Правило и спека

- [x] 1.1 Заархивировать выполненное изменение `client-and-core-module-split`; его capability
      `codebase-structure` переезжает в `openspec/specs/`. Проверка: `pnpm spec list --specs`
      показывает `codebase-structure`.
- [x] 1.2 Написать это изменение: proposal с материальным решением о зависимости, design с
      отвергнутыми альтернативами, дельта на `codebase-structure`. Проверка:
      `pnpm spec validate client-screen-routing --type change`.
- [x] 1.3 Переписать в `docs/CODE_STYLE.md` раздел «Где живёт файл»: раскладка перестаёт
      обосновываться отсутствием маршрутизации, появляется строка о том, где живут маршруты.
      Добавить правило «маршрут объявляется записью в таблице, а не веткой» и уточнение, что второй
      потребитель считается экранами, а не файлами.
- [x] 1.4 Обновить ссылки в `CLAUDE.md` и `AGENTS.md`; в блоке архитектуры `CLAUDE.md` дописать
      существующие `packages/client-shared` и `packages/config`. Проверка группы:
      `pnpm spec:validate` и `pnpm exec prettier --check docs/CODE_STYLE.md CLAUDE.md AGENTS.md`.

## 2. Маршруты консоли баланса

- [x] 2.1 Добавить `react-router` точной версией в `apps/admin/package.json`.
- [x] 2.2 Оболочка держит документ баланса, пароль и признак несохранённости и рендерит таблицу
      маршрутов; разделы получают их пропсами. `Outlet` с `useOutletContext` не используется: тогда
      каждый экран звал бы хук роутера, что правило запрещает.
- [x] 2.3 Вывести маршруты из существующей таблицы разделов, не заводя второго списка. Раздел
      статистики получает путь `/statistics`: на этом origin `/stats/` принадлежит API. Проверка:
      прямой заход на путь раздела открывает его, `data-testid` вкладок не изменились.
- [x] 2.4 Проверка группы: `pnpm typecheck`, `pnpm lint`, `pnpm build`, `pnpm format:check` — чисто;
      `pnpm test` — админка 69/69, три падения в `apps/server` предшествуют работе и воспроизводятся
      на чистом дереве. Контракт вкладок проверен в браузере вручную: клик по `admin-tab-*`,
      появление `admin-panel-*`, десять `nav.tabs button`, класс `tabs__tab--active`, прямая ссылка
      на `/statistics`, «назад», редирект `/` и неизвестного пути на `/waves`.
      `scripts/daily-video.mjs` пропущен по решению оператора: за дату с коммитами он идёт через
      `captureHistoricalUi`, то есть создаёт git worktree и ставит зависимости в копию.

## 3. Маршруты и раскладка контроллера

- [x] 3.1 Добавить `react-router` в `apps/controller/package.json`; маршруты `/` и `/room/:code`.
      Какой экран показать внутри комнаты — решает фаза из состояния комнаты, не адрес.
- [x] 3.2 `?room=CODE` продолжает работать и перенаправляет на `/room/CODE`; формат ссылки в QR не
      меняется. Проверка: `network-room.spec.ts` открывает контроллер только этой формой и проходит.
- [x] 3.3 Развести восемь корневых файлов: четыре с потребителями внутри одного экрана — в папку
      этого экрана, четыре модельных — в `model/`. Тесты переехали вместе. Корень `src/` теперь
      ровно то, что рисует `docs/CODE_STYLE.md`.
- [x] 3.4 Довести `App.tsx` контроллера до композиции и выбора маршрута: 575 строк → 100. Сеть — в
      `model/hooks/useControllerSession.ts` (369), фикстура — в `useControllerPreview.ts`,
      блокировка экрана — в `useScreenWakeLock.ts`, оболочка комнаты — в `screens/RoomScreen/`.
      Сессия о маршрутах не знает: она сообщает о выданном и потерянном месте, адрес двигает `App`.
- [x] 3.5 Проверка группы: `pnpm typecheck`, `pnpm lint`, `pnpm build`, `prettier --check` — чисто;
      `pnpm --filter controller test` — 62/62 vitest и 83/83 node; браузерные спеки, водящие
      контроллер (`network-room`, `pilot-helm`, `solo-room`) — 4 из 4, один тест пропущен своим
      `test.skip`.

### Открытый вопрос, вынесенный из 3.3

`JoinScreen`, `LobbyScreen` и `RoleScreen` лежат соседями, но лобби и роль — это две стадии одной
комнаты, а не два экрана: их единственный потребитель — `RoomScreen`. По лестнице они должны уехать
в `screens/RoomScreen/`. Это шестнадцать файлов, которых задача не называла, поэтому переезд не
делался. Решать отдельно.

## 4. Логика дисплея из `App.tsx`

- [x] 4.1 Фаза A — чистые листья: `model/environment.ts`, `model/roomFailure.ts` (+тест),
      `model/urlFlags.ts` (+тест), `model/moduleTree.ts`, `model/cockpitWorld.ts`,
      `model/hooks/useLetterboxBars.ts`. Шесть чтений `typeof window === "undefined"` стали одним.
- [x] 4.2 Фаза B — приборы: `model/liveView.ts` (+тест) и `model/instruments.ts`, плюс
      `model/hooks/useDiagnosticsMeters.ts`. Девять рефов, писатель и читатель которых расходятся по
      разным компонентам, живут в модуле, а не в пропсах.
- [x] 4.3 Фаза C — независимые эффекты: `useServerStatus` (каталог и обслуживание),
      `useRuntimePreload`, `useLiveHeat`, `useDisplaySwitches`. Переключатели отдают панели
      стабильные тумблеры вместо новой стрелки на рендер.
- [x] 4.4 Фаза D — сеть: `model/hooks/useViewPublisher.ts` и `model/hooks/useRoomSession.ts` (380).
      `createRoom` возвращает id комнаты, и хук ничего не знает об адресе. `App.tsx`: 1293 → 637
      строк.
- [x] 4.5 Проверка группы: `pnpm typecheck`, `pnpm lint`, `prettier --check` — чисто;
      `pnpm --filter display test` — 285/285; браузерная сюита — 7 живых спек из 8, `solo-room`
      падает на строке 124 под нагрузкой и трижды прошла в одиночку (13.6 с), то же было до правок
      дисплея. Сборка: чанк сцены остался отдельным (1.4 МБ против 746 КБ входа).
      `node scripts/bench-panels.mjs --plain --cpu=4`, три прогона: 155.5·0.0, 154.0·1.0, 155.5·0.0
      против записанных в `docs/performance/panels-against-the-reference-stand.md` 157.5·0.0 — рывки
      на нуле, к/с в пределах разброса трёх прогонов одной сборки. Отдельно с `?diag=1`: живы
      трафик, блокировки, «снимок → вид», коммиты React и покомпонентные стоимости — те четыре
      величины, которые не покрыты ни одним тестом.

## 5. Экраны и маршруты дисплея

- [ ] 5.1 Переименовать папку боевого экрана в экран комнаты: дерево покрывает и лобби, и бой.
- [ ] 5.2 Вынести панель управления превью вместе с её тестами, затем боевую сцену, затем оболочку
      экрана комнаты, затем маршрут превью. Публикующий фикстуру memo живёт в родителе боевого
      дерева и SHALL NOT переезжать в эффект.
- [ ] 5.3 Развести двадцать корневых файлов по ступеням лестницы по фактическому числу потребителей.
      Тесты переезжают вместе.
- [ ] 5.4 Добавить `react-router` в `apps/display/package.json` и маршруты `/`, `/room/:code`,
      `/preview`. Переход после создания комнаты сохраняет строку запроса и заменяет запись истории.
      Владелец сессии остаётся выше маршрутов.
- [ ] 5.5 Проверка группы: `pnpm check`; `pnpm --filter @spaceship-defender/display build` — сцена
      осталась отдельным крупным чанком; `node scripts/bench-panels.mjs --plain --cpu=4`; глазами
      параметры отладки и демонстрации.

## 6. Крупные модели дисплея

- [ ] 6.1 Разрезать словарь превью по фазам плюс общие фикстуры; публичные имена сохраняются.
      Проверка:
      `pnpm --filter @spaceship-defender/display exec vitest run src/model/previewMode.test.ts`
      проходит без правок теста.
- [ ] 6.2 Разрезать адаптер представления по коллекциям снимка с одним сборщиком наверху. Проверка:
      `pnpm --filter @spaceship-defender/display exec vitest run src/model/roomView.test.ts`.
- [ ] 6.3 Проверка группы: `pnpm check`.

## 7. Точка входа в сцену

- [ ] 7.1 Разрезать единственную точку входа в Phaser по причинам изменения: запуск, арена,
      сущности, прицеливание, щит, камера, счётчик кадров, запекание. Публичная поверхность
      сохраняется один в один, глобал камеры остаётся опубликованным.
- [ ] 7.2 Правило группы: один модуль — один коммит — `pnpm test:e2e` целиком, потому что
      единственная проверка правильности картинки здесь браузерная.
- [ ] 7.3 Проверка группы: `pnpm check`; глазами вариант арт-набора и плотность пикселей.

## 8. Уборка

- [ ] 8.1 Убрать мёртвые переключатели сцены, у которых не осталось вызывающих.
- [ ] 8.2 Решить судьбу ключей стенда замера, ссылающихся на удалённые переключатели: выкинуть или
      вернуть тумблеры. Решение оператора.
- [ ] 8.3 Убрать осиротевшие комментарии, оставшиеся от прежней раскладки `App.tsx`.
- [ ] 8.4 Два хука, стоящие у самого потолка размера, либо режутся, либо получают в шапке
      комментарий с причиной, почему деление хуже.
- [ ] 8.5 Проверка группы: `pnpm check`, `pnpm spec:validate`, и ни одного production-файла длиннее
      500 строк без обоснования в шапке.
