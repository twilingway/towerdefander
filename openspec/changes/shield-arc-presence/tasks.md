## 1. Геометрия пунктира во view-model

- [x] 1.1 Расширить `ShieldVisualStyle` описанием пунктира (`dash: { lengthPx, gapPx } | null`) и
      вернуть пунктир для выключенного состояния, сплошную линию — для активного.
- [x] 1.2 Добавить чистую `getShieldDashSegments(start, end, radius, dash)`: штрихи вдоль дуги,
      начало и конец сектора сохраняются, вырожденные радиус и угол дают пустой список.
- [x] 1.3 Покрыть тестами обе функции; проверить
      `pnpm --filter @spaceship-defender/display exec vitest run src/game/spaceshipViewModel.test.ts`.

## 2. Рисование и свечение

- [x] 2.1 Рисовать выключенный щит штрихами из `getShieldDashSegments`, активный — одной дугой.
- [x] 2.2 Включить фильтр Glow на объекте дуги при создании сцены и переключать его `active` по
      состоянию щита; при отсутствии списка фильтров у объекта тихо обойтись без свечения.
- [x] 2.3 Проверить `pnpm --filter @spaceship-defender/display exec vitest run`.

## 3. Проверка

- [x] 3.1 `pnpm check` и `pnpm spec:validate`.
- [ ] 3.2 Приёмка глазами: пунктир выключенного щита и ореол активного на реальном прогоне.
