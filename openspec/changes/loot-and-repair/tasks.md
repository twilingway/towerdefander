# Tasks

## 1. Опорный замер до правок

- [x] 1.1 Снять `pnpm benchmark:combat` и записать `pureFixedStep` p50/p95 сюда. До правок: p50
      `0.1399`, p95 `0.3066`, max `1.0053` мс.
- [x] 1.2 Прогнать `pnpm stats:batch` на фиксированных сидах (veteran, offset 0, экипаж 3, 16
      прогонов, сид 1) и записать сюда медиану волны, p95 и разбор причин поражения. Без этого числа
      шанс дропа настроить нечем. До правок: волна min 5, медиана 6, среднее 6.1, p95 8, max 8; все
      16 поражений — `spaceship_destroyed`; дошли до волны 1-5 по 16, до 6-й 10, до 7-й 7, до 8-й 1.

## 2. Сущность и дроп в ядре

- [x] 2.1 Завести `LootDropState` в `packages/game-core/src/combatTypes.ts` (`kind`, `amount` плюс
      поля `MovingEntity` и срок жизни) и добавить коллекцию в `CombatStateFields`. **Внести в белый
      список `pickCombatResult`** — он молча теряет неназванные поля.
- [x] 2.2 Добавить домен `LOOT_DOMAIN` в `combatConstants.ts` и поле состояния потока рядом с
      `ambientAsteroidRngState`.
- [x] 2.3 `createLootDrop` в `spawning.ts`: наследование скорости убитого врага с затуханием,
      номинал из конфигурации, вид по правилу «босс роняет ремонт».
- [x] 2.4 Бросок на смерти врага в `collisions.ts` — в той же ветке, где начисляются очки и кредиты,
      с проверкой потолка сущностей. Проверка: юнит-тест, что при шансе 0 лут не падает, при шансе 1
      падает ровно один, а босс роняет всегда.
- [x] 2.5 Движение, старение и вылет за границу в `threats.ts` / `collisions.ts` рядом с
      астероидами; лут в `dynamicEntityCount` и в `caps`.
- [x] 2.6 Магнит и подбор: вне радиуса дрейф, внутри ускорение к кораблю, касание корпуса —
      применение эффекта с клампом по максимуму. Проверка: тест на подбор ремонта при полном корпусе
      (лут расходуется, здоровье не превышает максимум) и на ячейку щита при опущенном щите.

## 3. Протокол и провод

- [x] 3.1 Ручки в `packages/protocol/src/balance.ts`: `lootChance` на `enemyArchetypeSchema`,
      глобальные `lootRepairAmount`, `lootShieldAmount`, `lootBossRepairAmount`,
      `lootLifetimeTicks`, `lootMagnetRadius`, `lootMagnetAccelerationPerSecondSquared`.
      `BALANCE_FILE_VERSION` +1, номер в `LEGACY_BALANCE_FILE_VERSIONS`.
- [x] 3.2 Миграция в `apps/server/src/balance/migrations.ts` по образцу `migrateAutopilot`: поле за
      полем, архетип за архетипом, ничего не переносить целиком. Проверка: регрессия в
      `balance.test.ts` — документ прошлой версии с ручной кампанией грузится, кампания цела, `warn`
      не вызывался; тест падает, если убрать миграцию.
- [x] 3.3 Схема лута в snapshot (`publicLootDropSchema`, коллекция в display view),
      `PROTOCOL_VERSION` +1. Проверка: `pnpm --filter @spaceship-defender/protocol test`.
- [x] 3.4 Зеркало в `SpaceshipDefenderState.ts` под `@view(1)` и синхронизация в
      `stateProjection.ts` через `reconcileKeyed`, ширины полей по правилу из `CLAUDE.md`.
- [x] 3.5 Адаптер `apps/display/src/roomView.ts` и превью-режим дисплея. Контроллер не трогать — он
      мира не видит.

## 4. Дисплей

- [x] 4.1 Отрисовка лута в `apps/display/src/game/` рядом с астероидами, вид отличим для ремонта и
      для ячейки щита, заметное поведение при притягивании.
- [x] 4.2 Маркер на радаре по образцу `radar-asteroid-markers`, отличимый от камня и от врага.

## 5. Консоль

- [x] 5.1 Поле шанса дропа на карточке врага в `apps/admin/src/screens/EnemiesScreen/`.
- [x] 5.2 Глобальные ручки лута на вкладке «Игрок» либо «Директор» — там, где живут остальные
      арены-и-корабля настройки. Проверка: `pnpm --filter @spaceship-defender/admin test`.

## 6. Балансировка и гейт

- [x] 6.1 Повторить замеры из задачи 1 и записать сюда обе пары чисел. Медиана волны обязана
      вырасти; если выросла больше чем на треть, шанс дропа слишком щедрый. **Волна:** медиана 6 →
      8, среднее 6.1 → 7.5, p95 8 → 9, max 8 → 9. Дошедших до 8-й волны 1 → 10 из 16, до 9-й 0 → 4.
      Причина поражения по-прежнему только `spaceship_destroyed`. **Шаг:** `pureFixedStep` p50
      0.1399 → 0.1496, p95 0.3066 → 0.3307, `pureFixedStepP95AtMost2Ms` истинно. Худший случай вырос
      на 12 сущностей (196 → 208), потому что лут теперь часть бюджета арены. **Оценка честно:**
      рост медианы ровно +33%, то есть на самой границе, которую эта же задача называет щедрой.
      Оставлено сознательно: цель дерева прокачки — 30 волн, а без лечения потолок был 6-7. Все
      ручки в консоли, следующая правка — по данным, а не по ощущению.
- [x] 6.2 Подобрать `lootChance` по архетипам батчами, а не на глаз; записать выбранные значения и
      измеренный результат. Взято: перехватчик `0.15`, gunship `0.22`, снайпер `0.25`, ракетоносец
      `0.3`, босс — ремонт всегда. Номиналы: корпус 35, щит 30, босс 200; время жизни 300 тиков (15
      с), радиус магнита 260, ускорение 900, затухание дрейфа 1.6.
- [x] 6.3 `pnpm check` и `pnpm spec:validate` с явным перехватом кода возврата. Результат:
      `format:check`, `lint`, `typecheck` чисто; protocol 78/78, game-core 195/195, admin 59/59,
      display 120/120, client-shared 10/10, server 184/185; `build` зелёный, `smoke:network` зелёный
      со второго запуска (первый — задокументированный таймаут ожидания передышки), `test:e2e` 5/5
      за 2.7 мин с кодом 0; `pnpm spec:validate` проходит. **Единственное падение —
      предсуществующее:** `SpaceshipDefenderRoom.test.ts` «publishes depletion and re-arms itself
      once the battery is back» даёт `energy: 5` вместо 0 и падает так же на полностью застэшенном
      рабочем дереве. Чинить в рамках `shield-timing`.

## 7. Ручная приёмка

- [ ] 7.1 `pnpm demo:visible`: виден ли лут на экране, понятно ли, что он притянулся, успевает ли
      пилот к нему до истечения срока жизни.
- [ ] 7.2 Взять босс-волну и убедиться, что большой ремонт читается как награда, а не теряется в
      куче обломков.

## 8. Окно сбора вместо зачистки в передышку

Правка принятого поведения: на приёмке выяснилось, что лут с последнего убитого не забрать — волна
кончается в тот же тик. Бесплатная выдача остатков экипажу убрана, вместо неё волна держится
открытой.

- [x] 8.1 Ручки `lootWindowTicks` (300) и `lootBossWindowTicks` (600) в пресете и в ядре,
      `BALANCE_FILE_VERSION` 25 → 26 с аддитивной миграцией по полям. Проверка: документ версии 24
      без обеих ручек получает значения по умолчанию и сохраняет свою таблицу волн — 44/44 в
      `apps/server/src/balance/balance.test.ts`.
- [x] 8.2 `openOrTickLootWindow` в `loot.ts` вместо `sweepLootDrops`: открытие в тик зачистки,
      босс-длина, если в этот тик умер босс, перезапуск часов дропов, досрочное закрытие на пустом
      поле. Поле `lootWindowTicksRemaining` в `CombatStateFields` и в белом списке
      `pickCombatResult`. Снаряды и ракеты убитой волны гасятся вместе с её открытием. Семь новых
      тестов в `loot.test.ts`, game-core 201/201.
- [x] 8.3 Провод: `lootWindowSecondsRemaining` в encounter view, `PROTOCOL_VERSION` 36 → 37, uint8 в
      схеме комнаты, оба roomView-адаптера.
- [x] 8.4 Дедлайн волны сдвигается на длину окна в момент его открытия
      (`extendWaveDeadlineForSalvage`, запас 2 с сверх окна), чтобы зачистка на последней секунде не
      превращалась в поражение по таймауту.
- [x] 8.5 Дисплей: `SalvageCountdown` вместо отсчёта до конца волны, пока окно открыто.
- [x] 8.6 Бот видит лут: `loot` и остаток окна в картинке мира обоих стендов, правило «лететь к
      ближайшему трофею в кадре, если он вернёт хотя бы половину своего номинала и крюк не длиннее
      700 единиц»; внутри окна берётся всё, что лежит.
- [x] 8.6.1 **Правка от 2026-08-31.** Сначала правило было ограничено окном сбора, и в задаче 8.7
      это объяснялось замером «в бою уход за дропом стоит дороже, чем дроп возвращает». Замер был
      негодный: обе сравниваемые сборки ещё несли баг с недобитым залпом, который перекрывал
      разницу. Пользователь увидел ровно это на экране — аптечка выпала, корпус побит, бот пролетел
      мимо. Снятие ограничения на тех же сидах: медиана волны 7 → **8**, среднее 6.4 → 7.1, max 9
      → 10. Заодно убраны шесть чтений статов корабля из конфига в картинке мира стенда — после
      `ship-stat-effects` они показывали боту неулучшенный корабль.
- [x] 8.7 Замер тем же стендом и теми же сидами (veteran, offset 0, экипаж 3, 16 прогонов, сид 1).
      Без лута (задача 1.2): медиана волны 6, среднее 6.1, max 8. Лут с бесплатной выдачей остатков
      (задача 6.1): медиана 8, среднее 7.5, max 9. Окно 15/30 с: медиана **7**, среднее 6.4, max 9,
      длина прогона медиана 453.4 с. **Честно: окно слабее бесплатной выдачи ровно на одну волну
      медианы** — выдача отдавала экипажу всё, что лежало на поле, включая ремонт босса на 200, а
      теперь за этим надо долететь. Бот летает за лутом плохо: он видит только кадр камеры и не
      имеет понятия «сходить за лечением», поэтому стенд теперь недооценивает лечение, а не
      переоценивает.
- [x] 8.7.1 Два побочных замера по дороге. Первый прогон окна дал медиану 5 — хуже, чем вообще без
      лута: волна перестала стирать вражеские снаряды и самонаводящиеся ракеты, и экипаж 15 секунд
      доедал последний залп. Исправлено вместе с окном (шутеры мертвы — их выстрелы умирают с ними),
      медиана вернулась к 7. Второй: окно 30 с для всех волн даёт ту же медиану 7 и среднее 6.8 —
      боту не хватает не времени, а намерения, поднимать длину окна незачем.
- [x] 8.7.2 `pnpm benchmark:combat`: `pureFixedStep` p50 `0.1402`, p95 `0.3329`, max `1.1548`,
      `pureFixedStepP95AtMost2Ms` истинно. Шаг не подорожал: окно не добавляет работы на тик.
- [x] 8.8 `pnpm check` с явным перехватом кода возврата: `format:check`, `lint`, `typecheck` чисто;
      protocol 78/78, game-core 201/201, admin 59/59, client-shared 10/10, display 122/122,
      controller 76/76 плюс 69 node-тестов политики, server 185/186. Гейт останавливается на том же
      предсуществующем падении `SpaceshipDefenderRoom.test.ts` «publishes depletion and re-arms
      itself» (`energy: 5` вместо 0, чинится в `shield-timing`), поэтому оставшиеся шаги прогнаны
      отдельно: `pnpm build` код 0, `pnpm smoke:network` код 0, `pnpm test:e2e` 5 passed за 2.6 мин
      код 0, `pnpm demo:verify` пройден целиком, включая `intermission` и `wave2`. Валидация
      изменения: `openspec validate loot-and-repair --strict` — valid.

## 9. Ремонт с босса долей корпуса

- [x] 9.1 Заменить абсолютный номинал `lootBossRepairAmount` долей эффективного корпуса
      `lootBossRepairShare`, поднять `BALANCE_FILE_VERSION` до 30 и отдать долю в консоль процентом.
- [x] 9.2 Тесты ядра на номинал и хранилища на пресет предыдущей версии.
- [ ] 9.3 Приёмка глазами: ремонт с босса вытягивает корпус с любого остатка.
