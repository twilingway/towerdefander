node_modules/
.pnpm-store/
dist/
coverage/
.turbo/
.vite/
.cache/
*.log

.env
.env.*
!.env.example
!.env.production.example

.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

apps/display/android/.gradle/
apps/display/android/local.properties
apps/display/android/app/build/

playwright-report/
test-results/

# Balance presets written by the admin console at runtime.
data/

# Local, reproducible source material for daily development Shorts.
artifacts/daily-videos/
.daily-worktrees/

# Snapshots a browser session drops beside the repo; never part of it.
.playwright-mcp/

# The reconstructed history of the project: its facts, its prose, its frames
# and the page built from them. Kept out for the same reason as the daily
# videos - tens of megabytes of pictures that would sit in the repository
# forever. It lives on this machine and in a published artifact instead.
artifacts/project-history/
